# Cosmos Policy 機構解析 統合検証レポート

**タスク**: PnPCounterToCab (RoboCasa)
**モデル**: `nvidia/Cosmos-Policy-RoboCasa-Predict2-2B`（EDM拡散フレームワークに基づく 28-block DiT、行動チャンクと将来画像ラテントを同時生成）
**主要な実行日**: 2026-06-30 〜 2026-07-06

## 0. 本レポートについて

本レポートは、Cosmos Policy（RoboCasaタスク上で行動を生成する拡散ポリシー）の内部機構を、
複数回にわたる検証・第三者レビュー・訂正のサイクルを経て解析した結果を、**単一の自己完結した
文書として統合**したものである。検証は大きく3ラウンドにわたって行われた：

1. **第1ラウンド**: サニティテスト・成功率ゲートの確立、行動出力（x̂₀）レベルのデノイジング動態、
   DiT内部表現（有効ランク・CKA・特徴ノルム）、線形プロービング、自己注意・クロスアテンション解析など、
   検証の大部分を実施した。
2. **第1ラウンドへの第三者レビュー**: 統合結論と個々の解析結果の間の矛盾、複数の指標の誤ラベリング
   （後述）、未実施の統制実験（T8）、交絡が分解されていない比較（T6）、pad マスクに関する検証不足などを指摘した。
3. **第2ラウンド**: レビュー指摘に対応し、T8統制実験の新規実施、T6交絡分解、PRの崖のσ解像度向上、
   J-b指標の再定義、pad寄与の直接数値照合などを行った。この過程で**新たな交絡（プローブ精度の
   run間差、PRのepisode数依存性）が副産物的に発見**された。
4. **第2ラウンドへの第三者レビュー**: 上記の新たな交絡が「レポート全体の三角測量的主張の土台を
   崩しかねない」規模であることを指摘し、根本原因の厳密な分解を要求した。
5. **第3ラウンド**: 指摘された交絡を全て厳密に分解し、**2つの重大な訂正**に到達した。
   (a) プローブ精度の「run間差」に見えていたものは、実際には単一runの中での **PCA前処理の選択
   （標準化してからPCAするか、中心化のみでPCAするか）という1つの決定論的要因**で全量説明できる
   ことが判明した。(b) `extended_step_effrank_jb.py` 系列のスクリプトが示す極端に低い有効ランク
   （PR≈2-4）は、**デノイジングの初期ノイズseedを全policy callで固定していた実装バグ**が原因で
   あることを特定し、修正・再検証した。

本レポートは、これら3ラウンドの検証結果のうち**最終的に正しいと確定した数値・結論**を主軸に据えつつ、
「当初どう考えられ、なぜそれが誤りだと分かったか」という訂正の経緯もあわせて記録する
（誤った初期解釈も、方法論上の教訓として保存する価値があるため）。全ての検証は、特記のない限り
**単一のチェックポイント**（後述 §2.3 で同一性を確認済み）に対して行われている。

**読み方の注意**: 本レポートで単に「PR」「CKA」「プローブ精度」等と言う場合、複数の異なる
ロールアウト実行（run）・エピソード数から得られた値が混在するため、**各表には必ず使用した
episode数・run（データセット）を明記する**。episode数・run が異なる値同士を直接比較しないこと
（§5 で詳述する通り、これらの量はrun・episode数に強く依存する）。

---

## 目次

1. [実験設定と検証基盤の健全性](#1-実験設定と検証基盤の健全性)
   - 1.1 [環境・モデル・データ構造](#11-環境モデルデータ構造)
   - 1.2 [サニティテスト（データ品質・フック健全性）](#12-サニティテストデータ品質フック健全性)
   - 1.3 [成功率ゲート（フックが行動生成に影響しないことの確認）](#13-成功率ゲートフックが行動生成に影響しないことの確認)
   - 1.4 [チェックポイント同一性の検証](#14-チェックポイント同一性の検証)
2. [テーマ1: 行動出力（x̂₀）レベルのデノイジング動態](#2-テーマ1-行動出力x0レベルのデノイジング動態)
   - 2.1 [Δx̂₀ 変化量と線形ガウスヌルとの対比](#21-δx0-変化量と線形ガウスヌルとの対比)
   - 2.2 [FFTスペクトル解析（周波数・位相）](#22-fftスペクトル解析周波数位相)
   - 2.3 [次元別変化量とグリッパー切替分析](#23-次元別変化量とグリッパー切替分析)
   - 2.4 [Preconditioning・測度集中（ε_θ と F_θ の混同の訂正）](#24-preconditioning測度集中εθ-と-fθ-の混同の訂正)
3. [テーマ2: 中間層内部表現の構造的崩壊（k=3→4）](#3-テーマ2-中間層内部表現の構造的崩壊k34)
   - 3.1 [有効ランク（Participation Ratio）の崩壊](#31-有効ランクparticipation-ratioの崩壊)
   - 3.2 [特徴ノルム・方向コサインの推移](#32-特徴ノルム方向コサインの推移)
   - 3.3 [ステップ間 CKA](#33-ステップ間-cka)
   - 3.4 [3指標のクロス一致と効果量](#34-3指標のクロス一致と効果量)
   - 3.5 [ランダム初期化nullモデルとの対比](#35-ランダム初期化nullモデルとの対比)
4. [テーマ3: PRのepisode数依存性とrun固有交絡の特定（方法論的検証）](#4-テーマ3-prのepisode数依存性とrun固有交絡の特定方法論的検証)
   - 4.1 [T6: 旧/新プローブ比較差の根本原因訂正](#41-t6-旧新プローブ比較差の根本原因訂正)
   - 4.2 [PRのepisode数学習曲線（run内での単離）](#42-prのepisode数学習曲線run内での単離)
   - 4.3 [崖→滑らかの再検証とseed固定バグの発見・修正](#43-崖滑らかの再検証とseed固定バグの発見修正)
5. [テーマ4: 線形プロービングによるタスク進行度・グリッパー状態のデコード](#5-テーマ4-線形プロービングによるタスク進行度グリッパー状態のデコード)
   - 5.1 [プロービング手法](#51-プロービング手法)
   - 5.2 [progress_3 / gripper_2 / gripper_3 の精度](#52-progress_3--gripper_2--gripper_3-の精度)
6. [テーマ5: 条件付け情報（画像・proprio）の伝播経路](#6-テーマ5-条件付け情報画像proprioの伝播経路)
   - 6.1 [自己注意解析（行正規化Tマトリクス）](#61-自己注意解析行正規化tマトリクス)
   - 6.2 [T8: 固定入力コントロール](#62-t8-固定入力コントロール)
   - 6.3 [T8b: gripperプローブの情報源テスト](#63-t8b-gripperプローブの情報源テスト)
   - 6.4 [Blk-18三角測量の総合評価](#64-blk-18三角測量の総合評価)
7. [テーマ6: 言語クロスアテンションとpadトークン](#7-テーマ6-言語クロスアテンションとpadトークン)
   - 7.1 [クロスアテンションの分布とpadトークンへの重み集中](#71-クロスアテンションの分布とpadトークンへの重み集中)
   - 7.2 [pad由来出力寄与のゼロ照合](#72-pad由来出力寄与のゼロ照合)
8. [テーマ7: 画像ラテント（将来フレーム）生成の解析](#8-テーマ7-画像ラテント将来フレーム生成の解析)
   - 8.1 [Δx̂₀ 変化量とラテントノルム](#81-δx0-変化量とラテントノルム)
   - 8.2 [FFT空間周波数解析](#82-fft空間周波数解析)
   - 8.3 [層間表現類似度（CKA）](#83-層間表現類似度cka)
   - 8.4 [線形プロービング](#84-線形プロービング)
9. [統合結論](#9-統合結論)
10. [既知の限界・未解決課題](#10-既知の限界未解決課題)
11. [発見されたバグ一覧](#11-発見されたバグ一覧)
12. [使用スクリプト・生成成果物一覧](#12-使用スクリプト生成成果物一覧)

---

## 1. 実験設定と検証基盤の健全性

### 1.1 環境・モデル・データ構造

**実行環境**: Singularity コンテナ（`--nv`）、`uv` 管理の Python 3.10 venv、GPU レンダリングは
EGL ヘッドレス (`MUJOCO_GL=egl`)。シミュレータは RoboCasa（robosuite ベース、PandaMobile ロボット）。

**モデルアーキテクチャ**: Cosmos Policy は EDM（Elucidating the Design Space of Diffusion-Based
Generative Models）拡散フレームワークに基づく 28-block の Diffusion Transformer (DiT) であり、
行動チャンク（32 timesteps × 7 次元: 位置XYZ・回転AxisAngle・グリッパー開閉）と、将来カメラ画像の
ラテント表現を**同時に**生成する。1回の推論（1 policy call）は複数のデノイジングステップ
（多くの検証では5ステップ、一部では9ステップに拡張）からなり、各ステップはノイズレベル σ の
Karras スケジュールに従う（典型的な5ステップでは σ = 80.0 → 42.3 → 21.0 → 9.6 → 4.0）。

**プローブ層**: 28 ブロックのうち Block-0, 4, 9, 13, 18, 22, 27 の7箇所を一貫してプローブ対象とした
（浅層・中間層・深層をカバーする代表点）。

**潜在系列構造（state_t=11 トークン位置）**:

```
T=0:  blank（バッファ、デノイジング対象外）
T=1:  現在proprio                    T=6:  将来proprio（生成対象）
T=2:  現在手首カメラ画像              T=7:  将来手首カメラ画像（生成対象）
T=3:  現在主カメラ画像                T=8:  将来主カメラ画像（生成対象）
T=4:  現在副カメラ画像                T=9:  将来副カメラ画像（生成対象）
T=5:  行動（生成対象、本レポートの主対象）
                                      T=10: value（生成対象）
```

各プローブ層の「特徴量」は、当該ブロック出力のうち行動トークン（T=5、空間パッチ方向に平均済み）
を指し、2048次元ベクトルである（画像トークンを扱う場合は該当 T 位置の特徴量を同様に抽出する）。

**EDM の preconditioning**: デノイズ後の予測は
`D_θ(x_t; σ) = c_skip(σ)·x_t + c_out(σ)·F_θ(x_t; σ)` と表される。ここで F_θ は前処理を外した
生のネットワーク出力、c_skip = σ_data²/(σ²+σ_data²)、c_out = σ·σ_data/√(σ²+σ_data²) であり
（Karras et al. 2022 の EDM 設計）、本モデルでは σ_data=0.5 が設計値として使われている。
ノイズ推定量（εパラメータ化）は ε_θ(x_t,σ) = (x_t − D_θ(x_t,σ)) / σ と定義される。

### 1.2 サニティテスト（データ品質・フック健全性）

解析はモデル内部にフォワードフックを挿入して特徴量・アテンション重みを捕捉する手法に依存するため、
フックそのものがモデルの計算やデータの整合性を壊していないことを最初に確認した。

| テスト | 内容 | 結果 |
|---|---|---|
| T1: フック有無の出力一致 | フックを挿入した状態としない状態でモデル出力（行動）が完全一致するか | **PASS**: max\|Δ_action\|=0.00e+00（閾値1e-4）。フック挿入前後で出力フィンガープリントも完全一致（`3dae305229c47f0f` ≡ `3dae305229c47f0f`） |
| T2: softmax手計算とSDPA実装の一致 | フックがQ,Kを再取得して `softmax(QKᵀ/√d)` を手計算した値が、モデル内部のScaled Dot-Product Attention実装と数値的に一致するか | **PASS**: max\|手計算−SDPA\|=5.36×10⁻⁷ |
| T4: 特徴量配列のハッシュ衝突なし | 保存された全特徴量配列（35個）が互いに異なるSHA256を持つか（同一テンソルの誤重複保存の検出） | **PASS** |
| T5: 形状一致 | 全 (k, layer) の特徴量配列が想定形状 (1108, 2048) を持つか | **PASS**（欠損・不整合なし） |
| T7: バッファ使い回しなし | 特徴キャプチャバッファが異なるステップ間で誤って再利用されていないか（同一層内でk間のハッシュが全て異なることを確認） | **PASS**（35セル全て固有ハッシュ） |
| T9: ノルム–コサイン–L2の数学的整合性 | `‖a−b‖² ≈ ‖a‖²+‖b‖²−2‖a‖‖b‖cos(a,b)` の恒等式が保存済み特徴量から実際に成り立つか（データの腐敗・欠損の検出に有効） | **PASS**: 最大相対残差 6.07×10⁻¹¹（許容範囲1e-3を10桁上回る精度） |
| T10: Provenance manifest | 生成された全成果物ファイルのSHA256を記録した manifest と実ファイルの整合性確認 | **PASS**（46ファイル追跡、存在しないファイル0件） |

### 1.3 成功率ゲート（フックが行動生成に影響しないことの確認）

T1（数値的な出力一致）に加え、実際のロールアウト成功率レベルでもフック挿入の影響がないことを
統計的に確認した。

| ラン | 成功率 | エピソード数 |
|---|---|---|
| G1: ベースライン（フックなし） | 62%（31/50） | 50 |
| G2: 特徴フックあり | 60%（30/50） | 50 |
| G3: 差分 \|p0−p1\| = 0.020、Fisher正確検定 p=1.000 | — | — |

**判定**: Fisher正確検定 p=1.000 ≫ 0.05 であり、フックの有無が成功率に統計的に有意な影響を
与えていないことを確認した（**PASS**）。以降の全解析はこの確認済みフック機構を用いている。

### 1.4 チェックポイント同一性の検証

**動機**: 検証の過程で環境ドリフト（ベース Video2World チェックポイントがHuggingFaceキャッシュから
一時的に失われ、再取得ルートに切り替わったイベント）が発生した。これが、複数の解析runの間で見られた
成功率・プローブ精度・有効ランクの変動（後述 §4 で詳しく扱う）の隠れた共通原因である可能性を
排除する必要があった。

**方法**: HuggingFace キャッシュを直接調査した。

- **ポリシー本体チェックポイント** (`nvidia/Cosmos-Policy-RoboCasa-Predict2-2B`): blob ハッシュ
  `ed4b991612b0d75e5e633fb2ca821e54c66a3b89973eb361e6ca50ba4984506f`（3,913,017,345 bytes）のみが
  存在し、キャッシュされたのは特徴量収集run（2026-06-30）よりも約3ヶ月前。スナップショットコミットは
  単一（`4b2a04c80d97202f86127ebec80461e8016ec1dc`）であり、以後の全実行時点でも同一のまま変化していない。
- **ベース Video2World チェックポイント** (`model-480p-16fps.pt`): こちらは実際に検証セッション中に
  再ダウンロードされている。しかしモデルロード用コードを確認すると、`config.checkpoint.load_path`
  （ベースチェックポイントを指す設定値）は、推論時に必ず実際に指定したチェックポイントパス
  （本解析では常にポリシーチェックポイント）で**上書きされる**ことが分かった。すなわち
  **ベースVideo2Worldチェックポイントの重みは推論経路では一度も読み込まれない**。ベース
  チェックポイントが実際に使われるのはトークナイザ（`tokenizer.pth` に相当する VAE 重み）としてのみであり、
  これは別ファイル・別の安定した凍結アーティファクトである。

**結論**: ポリシーの DiT 重みは、本レポートの全ての解析（複数回にわたる異なるロールアウトrunを含む）で
**単一の同一チェックポイント blob** により100%決定されている。ベースVideo2Worldチェックポイントの
再ダウンロードイベントはDiT重みに一切影響しない。したがって、**チェックポイントのdriftは、以降の
セクションで観測されるrun間変動の原因ではあり得ない**——原因は別の場所にある（§4 で特定する）。

---

## 2. テーマ1: 行動出力（x̂₀）レベルのデノイジング動態

> **要旨**: デノイジングが進むにつれて行動予測がどう変化するかを、デコード後の行動チャンク
> （x̂₀）レベルで定量化した。素朴な「Confidence-to-Commitment」仮説（後半のステップほど大きく
> 変化する＝確信が固まっていく）は、単に σ スケジュールが非線形であることの帰結である可能性を
> 排除するため、**線形ガウス最適デノイザをヌルモデルとして厳密に対比**した。結果、
> **最終ステップ（k=3→4）でモデルは線形ガウス最適推定よりもむしろ小さい変化しか起こしておらず
> （実測/ヌル比 0.48倍）、素朴な commitment 描像は行動出力レベルでは支持されない**。
> 一方、周波数解析（FFT）では低周波→高周波の順次精緻化（Coarse-to-Fine）も明確には観測されず、
> 次元別分析ではグリッパー次元の切替時振幅が最終ステップで顕著に増大することが分かった。
> また、ノイズ予測量 ε_θ のノルムがほぼ一定（CV=0.189%）という観測は、**高次元空間における
> 測度集中という純粋に数学的な帰結**であり、モデル固有の性質ではないことを検算で確認した
> （前処理を外した生のネットワーク出力 F_θ には意味のある状態依存の変動が確認される）。

### 2.1 Δx̂₀ 変化量と線形ガウスヌルとの対比

**目的**: デノイジングステップ k→k+1 での行動チャンク予測の変化量 ‖Δx̂₀‖ = ‖x̂₀(k+1)−x̂₀(k)‖ が、
ステップが進むにつれて単調に増加するか（＝終盤ほど大きく変化する「Confidence-to-Commitment」
パターン）を検証する。ただし単調増加が観測されても、それが「モデルが学習した振る舞い」なのか
「σ スケジュール自体が非線形（Karras スケジュールは終盤ほど σ の間隔が詰まる）であることの
数学的帰結」なのかは、単調増加という事実だけからは区別できない。そこで**モデルが何も学習して
いなかったとしても同じ σ スケジュールの下でどう振る舞うはずか**を表す理論的ヌルモデルと
対比することで、真にモデル固有の効果を切り出す。

**手法**: `x0_fn` をラップするフックで全デノイジングステップの x̂₀ を収集し、行動チャンク
(32 timesteps × 7 dims) にデコードした上で、連続ステップ間の L2 ノルム差を全 policy call
（N=1108、50エピソード、成功率60%）にわたって集計した。正規化は3方式で行った:
raw（生のノルム差）、relative（÷‖x̂₀‖）、schednorm（÷|Δlogσ|、σスケジュールの対数間隔で
割ることでスケジュールの粗密自体の影響を補正）。episode 単位のブートストラップ（n=1000
リサンプル）により95%信頼区間を算出し、「k=0→1 の変化量が k=3→4 より小さい」という仮説を
episode 単位の符号検定（各episodeでk=3→4の変化量がk=0→1を上回るかを2値化し、二項検定
p=0.5 を帰無仮説として検定）で確認した。

**ヌルモデルの構成（線形ガウス最適デノイザ）**: 行動分布が平均0・分散 σ_data² の等方ガウス
であるという最も単純な仮定の下で、ノイズ付き観測 x_t = x_target + σ·n（n は標準正規ノイズ）
から x_target を推定する最適（MMSE）デノイザは、Tweedie の公式により
`x̂₀(x_t,σ) = c_k · x_t`、`c_k = σ_data² / (σ_data² + σ_k²)` という線形形で解析的に書ける。
これは「モデルが行動分布についてガウス分布であるという最小限の情報以外何も学習していなかった
場合」に相当する最良のベースラインである。この解析式から `E[‖Δx̂₀‖]` を計算し、実測値と比較する
ことで、実測のプロファイル形状が単なるスケジュールの非線形性で説明できるのか、それを超えた
モデル固有の構造があるのかを判定できる。σ_data は x̂₀(k=4) の実測 per-component 標準偏差から
0.443 と推定した（D=32×7=224 次元）。

**結果**（episode平均、[95%CI]）:

| 正規化方法 | k=0→1 | k=1→2 | k=2→3 | k=3→4 |
|---|---|---|---|---|
| raw | 0.042 [0.040, 0.045] | 0.066 [0.060, 0.073] | 0.131 [0.112, 0.152] | 0.205 [0.172, 0.243] |
| relative（÷‖x̂₀‖） | 0.007 | 0.010 | 0.021 | 0.032 |
| schednorm（÷\|Δlogσ\|） | 0.066 [0.061, 0.073] | 0.094 [0.083, 0.108] | 0.168 [0.142, 0.198] | 0.234 [0.199, 0.273] |

episode単位の符号検定（k=0→1 vs k=3→4）: p < 10⁻¹⁵（全正規化方式で高度に有意）。

**線形ガウスヌルとの比（raw / schednorm いずれも同一比率）**:

| 遷移 | k=0→1 | k=1→2 | k=2→3 | k=3→4 |
|---|---|---|---|---|
| 実測（schednorm） | 0.066 | 0.094 | 0.168 | 0.234 |
| ヌル（schednorm） | 0.051 | 0.101 | 0.212 | 0.485 |
| **実測/ヌル比** | **1.29×（超過）** | 0.94× | 0.79× | **0.48×（過小）** |

**σ_data再較正の感度分析**: σ_data の推定値には実測std（0.443）とEDM設計値（0.5）の2通りが
考えられるため、`sigma_schedule=[80.0,42.3,21.0,9.6,4.0]`、D=224 の下で解析式によりヌル値を
再計算し感度を確認した。

| σ_data | ヌル(k=0→1) | ヌル(k=3→4) | 実測/ヌル比(k=0→1) | 実測/ヌル比(k=3→4) |
|---|---|---|---|---|
| 0.443（実測） | 0.0513 | 0.4845 | 1.29×（超過） | 0.48×（過小） |
| 0.5（EDM設計値） | 0.0654 | 0.6156 | 1.01×（超過ほぼ消滅） | 0.38×（より過小） |

**解釈**: schednorm 正規化後の値は k=0→1 から k=3→4 にかけて単調増加する（0.066→0.234、
p<10⁻¹⁵）が、**線形ガウスヌルも同様に単調増加する**（0.051→0.485）ため、単調増加という
事実自体はスケジュールの非線形性で説明可能であり、それだけでは「モデル固有の効果」の証拠には
ならない。しかし実測とヌルの**比の形状**はヌルと有意に異なる: 初期ステップ（k=0→1）では
実測がヌルを1.29倍**上回り**、最終ステップ（k=3→4）では実測がヌルの0.48倍しか**ない**
（=ヌルより小さい変化）。σ_dataをEDM設計値0.5に再較正すると、k=3→4の「過小」という中核結論は
0.48×→0.38×と**むしろ強化**される一方、k=0→1の「1.29倍の超過」という補助的な観察は
1.01×とほぼ消滅する。したがって k=0→1 の超過は較正に敏感な弱い観察であり独立した主張として
強調すべきではないが、**k=3→4でモデルが線形ガウス最適推定よりも小さい変化しか起こさない
という中核結論は較正の選択に対して頑健である**。

**結論**: 「デノイジング後半ほど大きく変化する」という素朴な Confidence-to-Commitment 仮説は、
**行動出力レベルでは支持されない**。むしろモデルは最終ステップにおいて線形ガウス最適推定よりも
小さい変化しか起こしておらず、これは行動分布が低次元多様体に集中しているため、最終ステップの
真の変化が線形理論の予測より小さくなることを示唆する。この「出力レベルでの抑制」は、後述する
「中間層の内部表現レベルでの k=3→4 の大きな再編」（§3）とは**独立の現象**であり、両者を
単一の「Confidence-to-Commitment」という名称で一括りに語ることは誤解を招くため避ける
（出力が収束しつつある一方で、内部表現の座標系が切り替わる、という状況はニューラルネットで
しばしば見られる非対称性である）。

**注意点・限界**: k=0→1 の「1.29倍の超過」という観察は σ_data の較正選択に敏感であり
（0.5への再較正でほぼ消滅）、独立した主張としては扱わない。σ_dataの真の値そのものについて
本解析で新たに推定・検証したわけではなく、実測stdとEDM設計値の2点で感度を確認したのみである。

**今後の検証案**: σ_data をより厳密に（例えば行動データセット全体の分散から）再推定し、
ヌルモデルの較正をさらに頑健にすることが考えられる。また、線形ガウスヌル以外の非線形ヌル
（例えば行動分布の低次元多様体構造を明示的に組み込んだヌル）との対比も、出力レベルの
「小さい変化」の解釈をさらに深める可能性がある。

### 2.2 FFTスペクトル解析（周波数・位相）

**目的**: デノイジングが「低周波成分（大まかな軌道の形）→高周波成分（細部）」の順に精緻化する
という Coarse-to-Fine（C2F）仮説が、行動軌跡の周波数領域で成立するかを検証する。

**手法**: 各 policy call の Δx̂₀（行動差分）を次元ごとに1次元 FFT し、次元平均のスペクトル
重心（パワーで重み付けした周波数の平均、値が大きいほど高周波成分が優勢）をステップごとに
算出した。k依存性の有意性は置換検定（B_B検定: 各 policy call 内で k ラベルをシャッフルし、
シャッフル後のスペクトル重心の範囲（max−min）の分布を n=1000 回のリサンプルで構築、実測の
範囲がこの帰無分布のどこに位置するかで p 値を算出）で確認した。

**結果**:

| 遷移 | スペクトル重心 |
|---|---|
| k=0→1 | 0.155 |
| k=1→2 | 0.167 |
| k=2→3 | 0.148 |
| k=3→4 | 0.150 |

B_B置換検定: 観測範囲 max−min=0.0181、p<0.0001（k依存性は統計的に有意）。ただし
Spearman順位相関（重心とkの単調な関係を見る補助的な検定）は ρ=−0.600, p=0.400 で非有意
であり、4点のみのデータで単調な傾向を主張するには検出力が不足している。

**補足（行動チャンク全体の周波数帯域分割、および位相解析）**: 各 policy call の x̂₀ 軌跡を
低周波域（f≤0.25）と高周波域（f>0.25）に分割してパワーを集計したところ、両域とも k=0〜k=3で
ほぼ変化せず、k=3→4でのみ微増した（低周波: 1.3185→1.3275、高周波: 0.1719→0.1786）。また
FFT位相から推定した時間シフト（k=0の軌跡とk=4の軌跡の間の時刻方向のズレ）は −0.011
timesteps とほぼゼロであり、デノイジングは軌跡の「タイミング」を変えず「値」のみを精緻化する
ことを示す。

**解釈**: 重心の k 依存性自体は統計的に有意（B_B検定 p<0.0001）だが、**k=1→2が最高、
k=2→3が最低という非単調パターン**であり、C2F仮説が予測する「kとともに単調に高周波化する」
という方向とは一致しない。この非単調性は4点中1点（k=1→2）の外れ値的な上昇に強く依存しており
（他の3点は0.148〜0.155のレンジに収まる）、Spearman相関が非有意であることとも整合する。
低周波・高周波のパワー分割やタイミング不変性の結果も、デノイジングが周波数帯域の順次精緻化
ではなく主に値の精緻化を行っていることを裏付ける。

**結論**: 行動空間 Δx̂₀ における Coarse-to-Fine 仮説は、**検出力不足ではなく、観測データが
積極的にこの仮説の予測方向と異なることを理由に支持されない**——ただし「積極的に逆方向」と
断定するのは4点中1点に強く依存した非単調変動への過度な踏み込みであり、正確には「重心は
kによらずほぼ平坦（≈0.15、白色ノイズの重心≈0.25よりは低周波優位）であり、単調増加という
C2Fパターンは検出されない」と述べるにとどめるのが適切である。

**注意点・限界**: k=4点のみの時系列であるため、非単調性の細かい形状に統計的な確証を与える
にはデータ点数が不足している（9-stepスケジュールなど、より高分解能な σ 軸での再解析が
望ましいが、本レポート執筆時点では周波数解析について9-step版の再実行は行っていない）。

**今後の検証案**: §4.3で用いた9-stepデノイジングスケジュールを用いて、周波数重心・帯域分割の
時間分解能を高めた再解析を行うことが考えられる。

### 2.3 次元別変化量とグリッパー切替分析

**目的**: 行動の7次元（位置XYZ・回転AxisAngle・グリッパー開閉）のうち、どの次元グループが
デノイジングによる変化を主に駆動しているかを特定する。特にグリッパー次元は二値的な開閉切替を
含むため、切替の有無で変化量を分解し、「切替の頻度が増えるのか、切替時の振幅が増えるのか」を
切り分ける。

**手法**: 保存済みの行動チャンク軌跡から、次元グループ別の RMS(Δx_t) を全 policy call
（N=1108, T=32, D=7）について計算した。グリッパー次元については、各 policy call 内で
グリッパー指令が閾値をまたいで切り替わっている（開↔閉）かどうかで呼び出しを2群に分け、
群ごとの RMS を別々に算出した。

**結果: 次元グループ別 RMS**:

| 次元/グループ | k=0 | k=1 | k=2 | k=3 | k=4 | k=0→4 変化率 |
|---|---|---|---|---|---|---|
| X (dim0) | 0.0500 | 0.0500 | 0.0501 | 0.0502 | 0.0518 | +3.5% |
| Y (dim1) | 0.0192 | 0.0191 | 0.0191 | 0.0191 | 0.0197 | +2.6% |
| Z (dim2) | 0.0545 | 0.0544 | 0.0547 | 0.0549 | 0.0568 | +4.2% |
| Rx (dim3) | 0.0087 | 0.0086 | 0.0086 | 0.0086 | 0.0087 | ±0% |
| Ry (dim4) | 0.0123 | 0.0122 | 0.0122 | 0.0121 | 0.0121 | −1.6% |
| Rz (dim5) | 0.0074 | 0.0073 | 0.0074 | 0.0074 | 0.0074 | ±0% |
| **Grip (dim6)** | **0.0975** | **0.0978** | **0.0988** | **0.1030** | **0.1154** | **+18.3%** |

**結果: グリッパー切替イベントの有無による分解**:

| k | 切替ありcall数（/1108） | 切替なしRMS平均 | 切替ありRMS平均 |
|---|---|---|---|
| k=0 | 400 | 0.0054 | 0.2626 |
| k=1 | 401 | 0.0052 | 0.2633 |
| k=2 | 399 | 0.0052 | 0.2678 |
| k=3 | 394 | 0.0053 | 0.2847 |
| k=4 | 386 | 0.0052 | **0.3329** |

**参考: 次元別の空間オフセット（k=0→k=4 の平均シフト、系統的バイアスの有無の確認）**:
x̂₀(k=4)−x̂₀(k=0) を全 policy call・全32 timestepにわたって平均した値（mean）とその標準偏差
（std）。mean≈0であれば系統的な方向バイアスはなく、stdが大きいほど状況依存の変化が大きいことを
意味する。

| 次元 | mean | std |
|---|---|---|
| dim0 (X) | +0.00211 | 0.0435 |
| dim1 (Y) | +0.00011 | 0.0130 |
| dim2 (Z) | −0.00083 | 0.0398 |
| dim3 (AxisAngle X) | +0.00054 | 0.0059 |
| dim4 (AxisAngle Y) | +0.00055 | 0.0086 |
| dim5 (AxisAngle Z) | +0.00009 | 0.0065 |
| dim6 (gripper) | +0.00166 | 0.0959 |

**解釈**: グリッパー切替イベントの**発生回数は k=4 でむしろ減少**する（400→386）にもかかわらず、
**切替時の振幅は k=4 で27%増大**する（0.263→0.333）。切替なしcallのRMSはk全体を通してほぼ一定
（≈0.005）。したがって、グリッパー次元全体のRMS増大（+18.3%）は「切替の頻度が上がる」ことでは
なく、「切替が起きるときの決断がより断定的（大振幅）になる」ことに起因する。空間オフセットは
全次元でmean≈0であり、系統的な方向バイアスは見られない——デノイジングは特定方向への固定シフト
ではなく、状況に応じた確率的な精緻化を行っている。

**結論**: 位置XYZとグリッパーが最終ステップで最大の変化量を示し（拮抗）、姿勢AxisAngleは
最小である。グリッパー次元の変化増大は「最終ステップで『グリップする/しない』の決断がより
強くなる」ことの表れであり、これは §2.1 で確立した「行動出力レベルでは素朴な commitment は
支持されない」という結論と矛盾しない——ここで見ているのは全体のノルム変化量ではなく、特定の
（二値的な）次元における条件付き振幅の増大という、より限定的な現象である。

**注意点・限界**: グリッパー切替の閾値化は単純な二値分類であり、切替の「強さ」の連続的な
定量化（例えば切替前後の値の差そのもの）とは異なる粒度の解析である。

**今後の検証案**: グリッパー切替振幅の増大が、対応する内部表現（§3で扱う中間層のPR崩壊・CKA
急落）とどの程度時間的・層的に対応しているかを直接照合することが考えられる。

### 2.4 Preconditioning・測度集中（ε_θ と F_θ の混同の訂正）

**目的**: デノイジングステップを通じてノイズ予測量のノルムがほぼ一定であるという観測が、
「モデルが学習した性質」なのか、「高次元空間における測度集中という数学的必然」なのかを
切り分ける。

**背景（当初の誤ラベリング）**: 当初、`noise_pred_norm`（≈371.4、変動係数CV=0.189%とほぼ
一定）を「‖D_θ‖（デノイズ後の予測 x̂₀ のノルム）」として報告していた。しかし実装コードを
追跡すると、この量は実際には `noise_pred_norm = ‖ε_θ‖ = ‖(x_t − D_θ(x_t,σ))/σ‖` であり、
**D_θ そのものではなく、εパラメータ化されたノイズ推定量**であることが判明した。c_skip≈0・
c_out≈0.5 という EDM のスケーリング関係から D_θ ≈ 0.5·F_θ となるはずであり、定数倍はCVを
変えないため、もし noise_pred_norm が本当に D_θ のノルムであれば、そのCVは F_θ のCV
（後述、7.6%）と一致しなければならない。しかし報告されたCVは0.189%であり、この数値的な
矛盾が誤ラベリングの発覚点となった。

**手法（検算による原因特定）**: x_t = x_target + σ·n（n は標準正規分布ノイズ）であり、σ が
十分大きい範囲では x_t は σ·n に支配される。D_θ による予測誤差が σ に比べて小さければ、
`ε_θ = (x_t−D_θ)/σ ≈ n + (x_target−D_θ)/σ ≈ n` となり、ε_θ は近似的に標準正規分布ベクトルに
なる。d 次元の標準正規ベクトルのノルムは、高次元における測度集中（ノルムの分布が
`‖n‖≈√d` の周りに強く集中する現象）により、変動係数が `CV(‖n‖) = std/mean ≈ 1/√(2d)`
という**モデルの中身に依らない純粋に数学的な公式**に従う。ここで d は生成潜在系列全体の
総次元数（state_t=11×空間パッチ×チャンネル）であり、DiTブロックの隠れ次元2048とは別の空間
であることに注意する。この d を実測ノルムの2乗（`d_inferred = noise_pred_norm.mean()² ≈
137,983`）として、公式の予測値と実測CVを比較することで、観測されたCVが測度集中で説明し尽くせる
かを検証できる。

**結果**:

```
d_inferred = noise_pred_norm.mean()² ≈ 137,983
1 / √(2 × 137,983) = 1 / √275,966 = 1/525.3 = 0.1904%
```

報告値 CV=0.189% とほぼ完全に一致する。

一方、EDM の preconditioning を外した生のネットワーク出力 F_θ = (D_θ − c_skip·x_t) / c_out
を直接測定すると:

| σ | c_skip | c_out | ‖F_θ‖/√d | ‖D_θ‖/√d | CV% |
|---|---|---|---|---|---|
| 80.0 | 3.9e-5 | 0.4999 | 1.5501 | 0.7750 | 7.62% |
| 42.3 | 1.4e-4 | 0.4997 | 1.5513 | 0.7756 | 7.61% |
| 21.0 | 5.7e-4 | 0.4986 | 1.5513 | 0.7756 | 7.61% |
| 9.62 | 2.7e-3 | 0.4993 | 1.5505 | 0.7757 | 7.61% |
| 4.00 | 1.54e-2 | 0.4961 | 1.5470 | 0.7765 | 7.58% |

ガウス測度集中の理論下限は `1/(2√d) = 0.135%`（あるいは `1/√(2d)=0.190%`、どちらの定数を
使っても桁は変わらない）であり、実測CV≈7.6%はこの下限の**約40〜56倍**である。

**解釈**: `noise_pred_norm`（=ε_θ）のCV≈0.189%は**情報量ゼロ**である——これはEDMのε
パラメータ化に伴う高次元測度集中の数学的帰結であり、D_θ自体の性質でもモデルが学習した
性質でもない。対照的に、F_θ のCV≈7.6%はガウス測度集中の理論下限を40〜56倍上回っており、
これは σ にも入力にも依存しない自明な量ではなく、**状態依存の構造を持つ、モデルが実際に
学習した非自明な変動**であることを意味する。またF_θとD_θの比 ‖F_θ‖/‖D_θ‖≈2.00 は
c_out≈0.5 の EDM スケーリング関係と整合する。

**結論**: 「ノイズ予測量のノルムがほぼ一定」という観測から「モデルはσに関わらず一定の
大きさの予測をしている」という趣旨の結論を導くのは誤りであり、これはεパラメータ化という
表現の選択がもたらす数学的な必然に過ぎない。Preconditioning解析で意味を持つのは、
前処理を外した生のネットワーク出力 F_θ のノルムの σ 不変性（1.547〜1.551で最大変動0.3%）
と、その変動係数が測度集中の理論下限を大きく超えること（7.6% vs 0.135%）の2点である。

**注意点・限界**: 本解析はノルムという1次統計量のみに基づいており、F_θの「状態依存の構造」の
具体的な中身（何が7.6%の変動を生んでいるか）までは特定していない。

**今後の検証案**: F_θのCV変動が、行動の内容（例えばグリッパー切替の有無）や進行度と相関するか
を追加で調べることで、この非自明な変動の意味をさらに明らかにできる可能性がある。

---

## 3. テーマ2: 中間層内部表現の構造的崩壊（k=3→4）

> **要旨**: §2で見た「行動出力レベルでは素朴なcommitmentは支持されない」という結論とは別に、
> **DiT中間層（Block-4, 9, 13）の内部表現は、最終デノイジングステップ（k=3→4）で大きく
> 再編される**ことを、有効ランク（PR）・特徴ノルム/方向コサイン・ステップ間CKAという3つの
> 独立した指標全てで確認した。中間層はk=0〜3でPR≈53〜59の高次元的な表現を保つが、k=4で
> PR≈18〜21まで急落し、CKAも0.999から0.73前後まで急落する。浅層・深層はこの区間で安定
> している。ランダム初期化モデルとの比較により、この崩壊が学習によって獲得された構造であり
> アーキテクチャの自明な帰結ではないことも確認した。**ただし、この節のPR絶対値は単一run・
> 50エピソードのデータに基づくものであり、絶対値そのものがepisode数に強く依存するという
> 別の限界が§4で判明している**（本節内でのk方向の相対比較は影響を受けない）。

### 3.1 有効ランク（Participation Ratio）の崩壊

**目的**: 各DiT層の行動トークン特徴空間が、デノイジングステップを通じて実質的に何次元分の
情報多様性を保持しているかを定量化し、「表現が高次元的な探索状態から低次元的な決定状態へ
収束する」というcommitment描像を内部表現レベルで検証する。

**手法**: 有効ランクの指標として Participation Ratio (PR) を用いる。層の特徴量集合の
共分散（またはグラム行列）の固有値を λ₁,λ₂,...とすると、
`PR = (Σᵢλᵢ)² / Σᵢλᵢ²` と定義される。PRは「分散が実質的に何個の直交方向に均等に
分布しているか」を表す連続的な量であり、1（全分散が単一方向に集中）からmin(N,D)
（全方向に均等分布）までの値を取る。ハードな閾値（例えば累積寄与率90%に必要な主成分数）
より固有値スペクトル全体になめらかに依存するため、ノイズに対して頑健という利点がある。
全1108 policy callの特徴量（1108×2048）からRandomized SVD（上位100成分）でPRの点推定を行い、
episode単位（N_ep≈22）のブートストラップ（n=1000）で95%信頼区間を、k=3→4の低下の有意性を
符号検定で確認した。

**結果**（50エピソード、N=1108 calls、単一run）:

| Layer | k=0 | k=1 | k=2 | k=3 | k=4 | k3→4の低下量 | p (符号検定) |
|---|---|---|---|---|---|---|---|
| Blk-0 | 13.2 | 12.9 | 12.4 | 12.0 | 11.9 | +0.14 | <0.0001 |
| **Blk-4** | **55.9** | **55.8** | **55.8** | **52.4** | **18.4** | **+34.04** | **<0.0001** |
| **Blk-9** | **56.9** | **56.9** | **56.8** | **53.1** | **18.9** | **+34.22** | **<0.0001** |
| **Blk-13** | **58.2** | **58.0** | **57.4** | **52.9** | **20.6** | **+32.23** | **<0.0001** |
| Blk-18 | 11.4 | 11.5 | 11.6 | 11.7 | 11.8 | −0.05 | 0.0003 |
| Blk-22 | 10.8 | 10.8 | 10.8 | 11.0 | 11.4 | −0.37 | <0.0001 |
| Blk-27 | 2.3 | 2.3 | 2.3 | 2.4 | 2.4 | +0.05 | <0.0001 |

**解釈**: 中間層（Blk-4/9/13）はk=0〜3ではPR≈53〜58と高次元だが、k=4で18〜21へと急崩壊する
（+32〜34ポイントの低下、全て p<0.0001）。浅層（Blk-0）は微減、深層寄り中間層（Blk-18, 22）は
k=4に向けてPRがわずかに増加する逆傾向を示す。最深層（Blk-27）はk全体を通じてPR≈2.3〜2.4と
一貫して最も低く、出力が常に低次元（2次元程度）に収束していることを示す。

**結論**: 中間層において「最終デノイジングステップで表現が高次元的な探索状態から低次元的な
決定状態へ移行する」という表現レベルのcommitment的パターンが、大きな効果量（後述§3.4で
Cohen's d≈1.8〜1.9）で確認された。

**注意点・限界**: 本節のPR絶対値は50エピソード・単一runのデータに基づく。§4.2で示す通り、
PRの**絶対値**はサンプルしたエピソード（=シーン）数に強く依存するため、本節の値を他の
episode数・run由来のPR値と直接比較してはならない。本節内の同一run内でのk方向の相対比較
（k=0〜3が高く、k=4で崩壊するという形状）はこの限界の影響を受けない。

**今後の検証案**: 複数シードでの再現性確認（後述§10）。

### 3.2 特徴ノルム・方向コサインの推移

**目的**: PRの崩壊が「表現の大きさ（ノルム）」の変化なのか「表現の向き（方向）」の変化
なのかを切り分ける。

**手法**: 各層の特徴ベクトルのノルム ‖feat(k)‖₂ と、連続ステップ間のコサイン類似度
cos(feat(k), feat(k−1)) を全 policy call 平均で算出し、episodeブートストラップで95%CIを付与した。

**結果: ステップ間コサイン類似度**:

| Layer | k=0→1 | k=1→2 | k=2→3 | k=3→4 |
|---|---|---|---|---|
| Blk-4 | 0.9995 | 0.9981 | 0.9795 | **0.8993** |
| Blk-9 | 0.9988 | 0.9964 | 0.9847 | **0.9219** |
| Blk-13 | 0.9989 | 0.9966 | 0.9862 | **0.9414** |
| Blk-18 | 0.9997 | 0.9990 | 0.9961 | 0.9855 |
| Blk-22 | 0.9999 | 0.9997 | 0.9984 | 0.9920 |
| Blk-27 | 1.0000 | 1.0000 | 1.0000 | 0.9996 |

**参考: 特徴ノルムの推移**（全policy call平均）:

| Layer | k=0 | k=4 | パターン |
|---|---|---|---|
| Blk-0 | 16.67 | 18.95 | 単調増加 |
| Blk-4 | 146.9 | 172.8 | k=4で急増 |
| Blk-9 | 157.9 | 155.6 | U字型 |
| Blk-13 | 166.6 | 195.4 | k=4で急増 |
| Blk-18 | 273.3 | 345.3 | 単調増加・急峻（+26%） |
| Blk-22 | 428.6 | 480.9 | 単調増加 |
| Blk-27 | 3931 | 3652 | 単調減少 |

**解釈**: 中間層（Blk-4/9/13）はk=3→4で方向コサインが0.90〜0.94まで低下し、これはPRの崩壊
（§3.1）と対応する「方向転換型」の再編を示す。一方、特徴ノルム自体は層によって全く異なる
挙動を示す——Blk-18は単調な急激なノルム増加（+26%）だが方向はほぼ維持（cos≈0.985）、
Blk-27はノルムが単調減少するが方向はほぼ完全に不変（cos≈0.9996）。これはBlk-27が
「収縮型」（予測を絞り込むが方向は変えない）、Blk-18が「スケール増大型」（方向を維持しつつ
大きさだけ増やす）という異なる振る舞いをしていることを示唆する。

**結論**: 中間層のPR崩壊は、ノルムの変化ではなく主に**表現方向の大きな転換**によって
駆動されている。この方向転換のパターンは層によって定性的に異なり、単一の「commitment」
という言葉で全層の挙動を代表させることは適切でない。

**注意点・限界**: ノルムと方向の変化は独立した指標として提示しているが、両者の間の
定量的な関係（例えばノルム増加が方向の不安定化とどう関係するか）までは踏み込んでいない。

**今後の検証案**: 特になし（本解析で当初の疑問には十分答えられている）。

### 3.3 ステップ間 CKA

**目的**: PR・コサインとは独立の指標として、線形 CKA (Centered Kernel Alignment) により
表現の幾何学的構造（サンプル間の相対的な配置関係）がステップ間でどれだけ保たれるかを測定する。

**手法**: 線形CKAは `CKA(X,Y) = ‖YᵀX‖²_F / (‖XᵀX‖_F · ‖YᵀY‖_F)` と定義され、0〜1の値を取り
（1が完全に同一の表現幾何構造）、直交変換や等方的スケーリングに対して不変という性質を持つ。
コサイン類似度が個々のベクトルの方向を見るのに対し、CKAはサンプル集合全体の相対的な配置
（どのサンプル同士が近いか）を見るため、より頑健な「表現の構造」の指標として機能する。
サンプルブートストラップ（n=200）による95%CIを付与した。

**結果**:

| Layer | k=0→1 | k=1→2 | k=2→3 | k=3→4 |
|---|---|---|---|---|
| Blk-0 | 1.000 | 1.000 | 0.997 | 0.964 |
| **Blk-4** | 0.999 | 0.997 | 0.962 | **0.729** |
| **Blk-9** | 0.999 | 0.997 | 0.962 | **0.740** |
| **Blk-13** | 0.999 | 0.997 | 0.965 | **0.765** |
| Blk-18 | 1.000 | 1.000 | 0.999 | 0.988 |
| Blk-22 | 1.000 | 0.999 | 0.998 | 0.987 |
| Blk-27 | 1.000 | 1.000 | 0.999 | 0.999 |

**解釈**: 中間層（Blk-4/9/13）はk=0→1, 1→2 ではCKA≈0.999（ほぼ恒等変換）だが、k=3→4で
0.73〜0.77まで急落する。深層（Blk-18, 22, 27）はk全体を通じてCKA≥0.987と表現構造がほぼ
変化しない。

**結論**: PR・コサインと完全に整合する形で、中間層のみが最終ステップで表現構造の大きな
再編を経験することが、独立した第3の指標（CKA）でも確認された。

**注意点・限界**: サンプルブートストラップのサンプルサイズ（n=200）は全体のcall数（1108）
より小さいサブサンプルであり、CIの幅にはこの点が反映されている。

**今後の検証案**: 特になし。

### 3.4 3指標のクロス一致と効果量

**目的**: PR・CKA・コサインの3指標が、k=3→4の再編について同じ方向の符号を示すかを
一括りに確認し、これが測定アーチファクトでなく実際の表現変化であることの頑健性を高める。
また各指標の変化の大きさを標準化された効果量で表現する。

**手法**: Cohen's d（対応のあるepisode単位の差、`d = mean(diff)/std(diff)`、慣習的に
0.2/0.5/0.8をsmall/medium/largeとする）でPR低下の効果量を評価した。CKAはepisode単位の量
ではないため、`d_proxy = (1−CKA_{k=3→4}) / SE_bootstrap` という「点推定がCKA=1.0（完全一致）
から何ブートストラップ標準誤差離れているか」を表す別種の量も参考値として記録するが、
**これはCohen's dと定義も解釈も異なる**ことに注意する。ブートストラップSEは独立サンプル数に
依存して縮小するため、SEが小さいほどd_proxyはいくらでも巨大になり得る——これは「統計的変動を
圧倒的に上回る効果」を意味するのではなく、単に「推定が正確である」ことを示すに過ぎない。
そのためd_proxyはCohen's dと並べて「効果量」として提示せず、推定の統計的安定性の確認
（有意性のプロキシ）としてのみ注記付きで扱う。

**結果: 3指標の符号一致（k=3→4）**:

| Layer | PR低下（§3.1） | CKA低下（§3.3） | コサイン低下（§3.2） | 符号一致 |
|---|---|---|---|---|
| Blk-0 | +0.14（小） | 0.964（中） | あり | ✅ |
| **Blk-4** | **+34.04（大）** | **0.729（大）** | **あり（大）** | ✅ |
| **Blk-9** | **+34.22（大）** | **0.740（大）** | **あり（大）** | ✅ |
| **Blk-13** | **+32.23（大）** | **0.765（大）** | **あり（大）** | ✅ |
| Blk-18 | −0.05（微小） | 0.988（安定） | ほぼなし | ✅ |
| Blk-22 | −0.37（微小） | 0.987（安定） | ほぼなし | ✅ |
| Blk-27 | +0.05（微小） | 0.999（安定） | なし | ✅ |

**結果: Cohen's d（PR drop, k=3→4, paired per-episode）**:

| Layer | d | 95%CI | 判定 |
|---|---|---|---|
| Blk-0 | 1.939 | [1.635, 2.362] | Large |
| **Blk-4** | **1.896** | [1.616, 2.285] | Large |
| **Blk-9** | **1.887** | [1.589, 2.259] | Large |
| **Blk-13** | **1.801** | [1.528, 2.168] | Large |
| Blk-18 | 0.625 | [0.364, 0.965] | Medium |
| Blk-22 | 0.670 | [0.378, 1.178] | Medium |
| Blk-27 | 0.223 | [−0.055, 0.621] | Small（CIが0を含む） |

（Blk-0のd=1.939が「Large」であることに注意：これはPRの**絶対変化量がわずか0.14**である
にもかかわらず生じる。統計的効果量（分散に対する相対的な差）は実務的な変化の大きさとは
別の指標であり、両者を併記せずにどちらかだけを見ると誤読を招く。）

**参考: d_proxy（CKA drop、有意性のプロキシとしてのみ）**:

| Layer | CKA (k=3→4) | SE | d_proxy |
|---|---|---|---|
| **Blk-4** | 0.729 | 0.0038 | 71.2 |
| **Blk-9** | 0.740 | 0.0037 | 70.2 |
| **Blk-13** | 0.765 | 0.0037 | 64.2 |
| Blk-18 | 0.988 | 0.0007 | 18.1 |
| Blk-27 | 0.999 | 0.0001 | 9.2 |

**解釈**: 全7層で3指標の符号が完全に一致しており、中間層k=3→4の急激な特徴変化は測定
アーチファクトではなく実際の表現変化であることが確認された。中間層のPR低下は Cohen's d≈1.8〜1.9
（Large）という大きな効果量を持つ一方、Blk-27のPR変化は統計的には検出されない
（CIが0を含む）。d_proxyは推定の安定性の参考にはなるが、Cohen's dのような実務的な効果量の
指標としては用いない。

**結論**: 中間層のk=3→4再編は、複数の独立な指標・大きな効果量によって裏付けられた頑健な現象
である。

**注意点・限界**: これらの指標は全て単一run（50episode）のデータに基づく。

**今後の検証案**: 複数シードでの再現性確認（後述§10）。

### 3.5 ランダム初期化nullモデルとの対比

**目的**: 上記で観測した特徴構造（中間層でのPRの高さ、k=3→4での崩壊、深層でのPRの低さ）が
「学習によって獲得された構造」なのか、「DiTアーキテクチャそのものの自明な帰結（学習していなくても
生じる）」なのかを切り分ける。

**手法**: 学習済みモデルと同一のDiTアーキテクチャのパラメータを、平均0・標準偏差0.02の
正規分布でランダムに再初期化した（seed=42）。この「nullモデル」で同一タスク・10エピソード
分の行動トークン特徴を収集し、trained（学習済み）モデルのPRと全 (layer, k) の組み合わせで
比較した。

**結果**（N=10 episodes, 320 calls）:

| Layer | k | PR(trained) | PR(null) | trained/null |
|---|---|---|---|---|
| 0 | 0 | 13.2 | 4.88 | 2.7× |
| **4** | 0 | **55.9** | **3.76** | **14.9×** |
| **9** | 0 | **56.9** | **3.58** | **15.9×** |
| **13** | 0 | **58.2** | **3.51** | **16.6×** |
| 18 | 0 | 11.4 | 3.53 | 3.2× |
| 22 | 0 | 10.8 | 3.54 | 3.0× |
| **27** | 0 | **2.30** | **3.54** | **0.65×（学習済みの方が低い）** |
| 4 | 4 | 18.4 | 3.74 | 4.9× |
| **9** | 4 | **18.9** | **3.54** | **5.3×** |
| **13** | 4 | **20.6** | **3.48** | **5.9×** |
| **27** | 4 | **2.35** | **3.48** | **0.68×（学習済みの方が低い）** |

**解釈**: ランダム初期化モデルのPRは全層・全kで一定して低い（≈3.5〜4.9）——理論的には
ランダムなアフィン変換の連鎖であればPRは埋め込み次元2048に近い値になると予想されるが、
実際には深いMLP＋LayerNormの連鎖がアーキテクチャ的に低有効ランクへの崩壊をもたらすことが
示唆される。中間層（Blk-4/9/13）ではtrained PRがnull PRを大きく上回る（k=0で最大16.6倍、
k=4でも5〜6倍）——これは学習によって中間表現の多様性が積極的に増大したことを意味する。
対照的にBlock-27（最終出力層）ではtrained PR(≈2.3)がnull PR(≈3.5)を**下回る**——これは
学習によってアクション出力が特定方向に集中する（さらなるPR圧縮）ことを意味し、これも
学習構造の証拠である。k=3→4でのtrained PRの急落（Blk-4: 52.4→18.4等）はnullモデルでは
観察されない急落であり、この最終ステップ固有のPR変化が学習固有の構造であることを示す。

**結論**: §3.1〜3.4で確認した中間層のPRの高さおよびk=3→4での崩壊、深層での低PRは、
いずれもDiTアーキテクチャ単体の自明な帰結ではなく、**学習によって獲得された表現構造**である。

**注意点・限界**: このnullモデル比較はN=10エピソード（320 calls）という小標本に基づいており、
§4.2で示すPRのepisode数依存性を踏まえると、この規模のデータで測定したPR絶対値（trainedも
nullも）自体にも同様の episode 数依存のバイアスがかかっている可能性がある。ただし
trained/null比較は同一のepisode数・同一のタスク設定で行っているため、両者が同じ方向・
同程度のバイアスを受けていれば比較の妥当性は保たれると考えられるが、これ自体は本レポートでは
直接検証していない。

**今後の検証案**: nullモデル比較についても、§4.2と同様のepisode数感度分析を行い、trained/null
比の値がepisode数に依存しないことを確認することが望ましい。

---

## 4. テーマ3: PRのepisode数依存性とrun固有交絡の特定（方法論的検証）

> **要旨**: 検証を重ねる過程で、「同一の手法・同一のラベル定義で計算したはずのプローブ精度が、
> 異なるロールアウト実行（run）の間で7〜20ポイントも異なる」「異なるスクリプトで計算した
> 有効ランク（PR）が、同じ50エピソード相当のはずなのに55前後と2〜4という10倍以上異なる値を
> 示す」という、当初原因不明の大きな不一致が2つ発見された。本テーマは、この2つの不一致を
> それぞれ根本原因まで遡って特定した過程を記録する。結論として、(1) プローブ精度の見かけ上の
> 「run間差」は、実際には**単一runの中でのPCA前処理の選択（標準化してからPCAするか、
> 中心化のみでPCAするか）という1つの決定論的要因**で全量説明できることが判明した。
> (2) 極端に低いPR値は、**あるスクリプトが全policy callでデノイジングの初期ノイズseedを
> 固定してしまっていた実装バグ**が原因であり、修正後は正しい絶対値が復元された。
> 両者に共通する教訓として、**PRの絶対値は実際に「何episode（=何種類のシーン）を
> サンプルしたか」に強く依存する**ことも確認された（真のrun間差ではなく、いずれも
> 単一のシンプルな原因に帰着する見かけ上の不一致だった）。

### 4.1 T6: 旧/新プローブ比較差の根本原因訂正

**目的**: 検証の初期段階で、線形プロービングの実装を「Global PCA（全データでPCAを1回計算、
テストデータもPCA計算に混入するためリークあり）+ Ridge回帰」から「fold内PCA（LOEOの各foldで
訓練データのみからPCAを再計算、リークなし）+ LogisticRegression」に変更したところ、中間層
（Blk-4/9/13）の精度が+11〜21ポイントも改善するという逆説的な結果が観測された。一般に
データリークの除去は（テストへの情報混入がなくなる分）精度を**下げる**方向に働くはずであり、
逆に上がったことの原因を特定する必要があった。

**手法（第1段階、誤った結論に至った経緯）**: 新規スクリプトで、同一のLOEO split・同一ラベル
（progress_3, k=4）に対し `{PCA: Global(リークあり)/Fold(リークなし)} × {分類器:
Ridge/LogisticRegression} × {n_comp: 30/50}` の2×2×2=8セルを計算し、主効果を分解する
アブレーションを実施した。

**結果（第1段階）**: 8セルの精度差（Global+Ridge vs Fold+LR の合計Δ）は全層で−2.1pp〜+1.4ppに
収まり、ブートストラップCIの幅（±5〜6pp）にほぼ収まった——すなわち、**同一runのデータ内で
PCA・分類器・n_compを変えても精度はほぼ動かない**ことが分かった。これは当初のプロービング
比較（Blk-4: +16.6pp, Blk-9: +20.6pp, Blk-13: +11.3pp）と全く整合しない。原因を追跡したところ、
「旧」プローブの数値の出典（`probe_stats.json`、成功率0.70と記載）と「新」プローブの数値の
出典（`features.npz`、成功率0.60と記載）で**成功率の値そのものが異なる**ことに気づいた。
両者は同じN=1108 call・50 episodesという体裁だが、成功率という post-hoc の分析選択では
変わらないrunレベルの結果指標が異なる以上、**実際には異なるロールアウト実行（run）から
得られた別々の特徴量データだった**という仮説が立てられた。この仮説の下で「旧手法の処理
（Global PCA n=50+Ridge）を新runの特徴量に対して再現」すると、Blk-4: 0.594, Blk-9: 0.638,
Blk-13: 0.645 となり、当初「旧手法の結果」として報告されていた値（Blk-4: 0.435, Blk-9: 0.438,
Blk-13: 0.569）と7〜20ポイントの差があることが確認され、「run差が支配的要因」という結論に
達した。

**この結論への疑義と再検証（第2段階、真の原因の特定）**: 「旧」プローブの実装コードの
ヘッダを確認すると、実行例として現在の`features.npz`と全く同じファイルを読むよう設計されて
いることが分かった。`--success_rate`はプロットのタイトル注釈にのみ使われる**手動指定のCLI
引数**であり（デフォルト値0.70）、データから自動算出される値ではない。すなわち
`probe_stats.json`内の`success_rate: 0.70`は、**実データを反映しない古いデフォルト値の
書き残し**であり、"別runの証拠"ではなかった可能性が浮上した。これを実証するため、「旧」
プローブの実装コードを現在の`features.npz`/`step_actions.npz`に対してそのまま再実行したところ、
progress_3, k=4で次の結果を得た:

| Layer | 再実行した実際の旧コード | 元の`probe_stats.json`の値 | 差 |
|---|---|---|---|
| Blk-4 | 0.44 | 0.435 | ≈0 |
| Blk-9 | 0.44 | 0.438 | ≈0 |
| Blk-13 | 0.57 | 0.569 | ≈0 |

**同一の単一`features.npz`から、実際の旧コードで元の報告値をほぼ完全に再現できた**——
つまり「別run」という前提そのものが誤りであり、実際は常に同一の単一データセットだった。
`episode_labels`配列（各callがどのepisode(0-49)由来かを示す配列）を直接照合しても、旧コードの
実行ログと`features.npz`はいずれも同じ`episodes=[0...49]`を表示しており、同一データであることが
直接確認された。

**手法（第3段階、真の要因の特定）**: 「同一データなのに旧コードと新コードで数値が違う」という
事実を説明するため、両実装を1行ずつ比較したところ、2つの前処理の相違点が見つかった:

- **要因A（PCA入力のスケーリング）**: 旧コードの`pca_project`は生の2048次元特徴量を
  **中心化のみ**（`X_c = X − X.mean()`）してからSVDする。一方、第1段階のアブレーション
  スクリプトの`pca_global_leaked_fit`は**標準化（z-score）してから**（`X_s = (X−μ)/σ`）
  SVDしていた。
- **要因B（Ridge直前のスケーリング）**: 旧コードの`ridge_probe`はPCA後の50次元スコアを
  訓練foldで再標準化してからridgeに渡す。第1段階のアブレーションスクリプトの
  `ridge_fit_predict`はPCAスコアをそのまま渡す（再標準化なし）。

この2要因を独立に切り替える2×2アブレーションを`features.npz`に対して直接実行した
（progress_3, k=4, n_comp=50, 全7層でLOEO再計算）:

| Layer | A=中心化,B=fold再標準化<br>（旧コードを完全再現） | A=標準化,B=なし<br>（第1段階アブレーションの参照セル） | A=中心化,B=なし | A=標準化,B=fold再標準化 |
|---|---|---|---|---|
| Blk-0 | 0.5482 | 0.5544 | 0.5486 | 0.5544 |
| Blk-4 | **0.4352** | **0.5939** | 0.4352 | 0.5939 |
| Blk-9 | **0.4376** | **0.6378** | 0.4376 | 0.6378 |
| Blk-13 | **0.5689** | **0.6451** | 0.5689 | 0.6451 |
| Blk-18 | 0.7028 | 0.7053 | 0.7028 | 0.7053 |
| Blk-22 | 0.7237 | 0.7196 | 0.7237 | 0.7208 |
| Blk-27 | 0.7224 | 0.7324 | 0.7224 | 0.7324 |

**結果**: 決定的な観察は次の4点である。(1) 「A=中心化,B=fold再標準化」列は`probe_stats.json`
の元の値（0.435, 0.438, 0.569）と小数点以下まで一致する——これが旧コードの真の挙動の完全な
再現であることの確証。(2) 「A=標準化,B=なし」列は第1段階アブレーションの「旧手法参照値」
（0.594, 0.638, 0.645）と一致する——これが第1段階の実装がしていたことの確証。(3) 要因B
（Ridge直前の再標準化の有無）は結果に一切影響しない（「A=中心化,B=fold再標準化」列と
「A=中心化,B=なし」列が完全に同一の数値）。(4) 要因A（PCA前に標準化するか中心化のみかどうか）
だけで、全ての差が説明できる（Blk-4: +15.9pp, Blk-9: +20.0pp, Blk-13: +7.6pp——当初報告の
"+11〜21pp"にほぼ一致）。

**なぜPCA前の標準化がこれほど精度を変えるのか**: 各層の生特徴量の分散は層間で大きく異なり
（Blk-0で1.9、Blk-27で210141程度）、同一層内でも2048次元間の分散はおそらく大きく偏っている。
標準化せずにPCAすると、上位主成分は「分散が大きい少数の次元」に支配され、それが必ずしも
分類に有用な方向とは限らない。標準化してからPCAすると、全次元が等しく寄与するため、分類に
有用な方向がより多く上位主成分に残る——これはPCA前処理における広く知られた「whitening vs
non-whitening」の効果であり、run やロールアウトの違いとは無関係の、純粋に決定論的な前処理
選択の結果である。

**解釈**: 当初「リーク除去は通常精度を下げるはずなのに中間層で逆に精度が上がった」という
逆説として提起された問題は、実際には「リーク除去」や「分類器変更」が原因ではなく、
「PCA前の標準化」という当初見落とされていた第4の要因が原因だった。標準化してからPCAすると
Ridge/LogisticRegression・n_comp30/50によらず一貫して精度が高くなる。また `success_rate`
フィールドをrunの識別子として使うのは危険である（CLI引数のデフォルト値がそのまま保存される
ケースがある）——run同一性を確認する際は、`episode_labels`配列そのものの一致など、実データに
根ざした照合を行うべきである。

**結論**: 「旧/新プローブ比較は異なるrunだった」という第1段階の結論は誤りであり撤回する。
実際には**単一の同一run・同一features.npz**の中で、PCA前処理（標準化 vs 中心化）という
1つの選択が+7〜20ppの差の**全量**を説明する。この訂正の過程は、「厳密な要因分解を行う
べきだ」という方法論的な要求が完全に正しく、その結果、当初考えられていたよりもさらに単純で
ありふれた原因（1行のPCA前処理の選択）にたどり着いた例である。

**注意点・限界**: 本節の教訓（run識別子としての`success_rate`フィールドの危険性、PCA前処理の
選択が精度に与える影響の大きさ）は、この2つのプローブ実装に固有の問題ではなく、線形プロービング
一般に適用されうる注意点である。

**今後の検証案**: 標準化ありPCAを標準の前処理として採用し、以降のプロービング解析
（§5）は全てこの設定に統一することが望ましい。

### 4.2 PRのepisode数学習曲線（run内での単離）

**目的**: 有効ランク（PR）の絶対値が、真に「モデルの表現の多様性」を表しているのか、それとも
単に「何エピソード（＝何種類の異なるキッチンシーン）をサンプルしたか」という統計的なサンプル
サイズの効果を反映しているに過ぎないのかを、**同一run内で**厳密に切り分ける。RoboCasaは
エピソードごとに異なるキッチンレイアウト・物体配置を持つため、行動トークン表現の変動の一部が
「シーンの違い」に由来する可能性がある。この効果を単離するには、**同一runのデータをそのまま
episode数だけを制限して再計算する**必要があり、別のrunと比較する（＝episode数とrun差が
交絡する）方法では正しく単離できない。

**手法**: `features.npz`には各callがどのepisode(0-49)由来かを示す`episode_labels`配列が
既に保存されていることを利用し、**runを一切またがない**episode単位サブサンプリング学習曲線を
作成した。episode数 n∈{2,3,5,10,15,20,25,30,40,50} それぞれについて、50エピソードからn個を
非復元抽出し（n<50は30回リサンプル、n=50は全データそのもの）、該当callの特徴量でPRを計算した。

**結果**（k=0, Blk-4の例。全7層・全5ステップで同様の傾向）:

| n_episodes | 2 | 3 | 5 | 10 | 15 | 20 | 25 | 30 | 40 | 50（全データ） |
|---|---|---|---|---|---|---|---|---|---|---|
| 平均PR | 25.2 | 30.8 | 39.3 | 46.8 | 50.2 | 52.3 | 53.4 | 54.1 | 55.2 | **55.9** |
| 95%CI | [18.2,31.5] | [24.9,37.9] | [33.7,44.1] | [41.1,50.9] | [48.1,52.2] | [50.7,53.9] | [52.5,54.6] | [53.1,55.0] | [54.5,55.7] | (全データ) |

**解釈**: 滑らかで単調な飽和曲線であり、n=50（全データ）でPR=55.89に達する（§3.1で報告した
「PR≈55.8」の値そのものであり、同一データなので当然一致する）。n=25の時点で既にCIが
[52.5,54.6]まで収束しており、この良いrunの実データを25エピソードに縮約しても、PRは3〜4のような
値には決して落ちない。

**結論**: PRの絶対値は真にepisode（シーン）数に依存し、25エピソード程度で既に元の値の
約95%程度（53/55.9）まで収束する飽和曲線を描く。この「episode数依存性」自体は方向として
正しい効果だが、**次節で見るように、この学習曲線だけでは説明できないほど極端に低いPR値
（≈2-4）が、別の系列のスクリプトから観測される**ことが後に判明する。

**注意点・限界**: この学習曲線はk=0, Blk-4の1例のみを示したが、全層・全ステップで同様の
傾向が確認されている。episodeのサブサンプリングは非復元抽出・30回リサンプルであり、
実際のロールアウト（異なるシードでの新規実行）によるepisode数の効果とは、厳密には
異なる可能性がある（後者はエピソードの実現内容自体が変わるため）。

**今後の検証案**: 実際に異なるepisode数で新規ロールアウトを行い、本節のサブサンプリング
学習曲線と一致するかを確認することが望ましい。

### 4.3 崖→滑らかの再検証とseed固定バグの発見・修正

**目的**: §3.1で観測した中間層のPR急落（k=3→4）が、5ステップスケジュールの解像度不足による
見かけ上の「崖」なのか、それとも σ のより滑らかな関数としての連続的な減少なのかを、
中間σを挿入した高解像度スケジュールで切り分ける。

**手法（第1回、25エピソード）**: `num_denoising_steps_action`を5→9に増やし（同一
σ_max=80, σ_min=4のKarras ρスケジュールを密にする）、25エピソードのロールアウトでPRをσの
関数として高分解能に追跡した。

**結果（第1回）とその異常**: このrun（N=552 calls, 成功率60.0%）で得られたPR絶対値は、
§3.1の50-episode runの値（Blk-4≈55.8）よりずっと小さい値（Blk-4≈3-4）となった。当初
§4.2の学習曲線（episode数への依存性）で説明できるかを検討したが、学習曲線ではn=25で
PR≈53程度になるはずであり、実測の3〜4とは全く整合しない、約15倍の未説明のギャップが
残ることが判明した。

**手法（第2回、50エピソードへの拡張）**: episode数が原因であるかをさらに厳密に確認するため、
同一スクリプトのepisode数だけを25→50に変更して再実行した（seed=195, 9-stepスケジュール、
§1.4で同一性確認済みのチェックポイント）。

**結果（第2回）**: 50 episodes, 1300 policy calls, 成功率42.0%（元の60%runとは異なる値——
後述するseed固定バグの影響で行動生成が状況に適応せず同一の初期ノイズに固定されていたことが
一因である可能性が高い）。episode数を25→50に倍増させても、Blk-4のPRは3.2程度（25episode版の
3〜4とほぼ同じ）にとどまり、§4.2で確認した「良いrunから実際に25エピソードを縮約した場合の
PR≈53」には全く近づかなかった。すなわち、episode数を増やしても低いPRのまま変わらないという
事実は、「PRはepisode数に依存する」という§4.2の説明では、この系列のスクリプトが示す極端に
低いPR（≈2-4）を全く説明できないことを意味していた。原因はepisode数ではなく、この
スクリプト系列固有の何らかの方法論的な違いにあるはずであった。

**手法（根本原因の特定）**: `get_action()`関数は`seed`引数をモデルのサンプル生成関数に直接
渡し、これがデノイジングの**初期ノイズ x_T のサンプリングそのもの**を制御する乱数生成器のシード
として使われる。ここで、良いrun（50-episode特徴量収集で使われたスクリプト）と問題のスクリプトの
`seed`引数の与え方を比較したところ、決定的な違いが見つかった:

- 良いrun: `seed = cfg.seed + ep_idx + t` —— **ほぼ全てのpolicy callで異なる値**
  （episode番号とタイムステップ番号の両方で変化する）。
- 問題のスクリプト（当時のバージョン）: `seed = cfg.seed` —— **全てのpolicy callで完全に
  同一の固定値**。

すなわち問題のスクリプトは、**全てのepisode・全てのcallにわたって、デノイジングの初期ノイズ
x_T を毎回ビット単位で完全に同一のテンソルに固定していた**。これが真因であることを、シーン
多様性を一切変えないクリーンな実験で実証した: 1回のclosed-loopロールアウト（12 episodes,
N=352 calls）で観測プールを1度だけ収集し、**全く同一の観測集合**を (A) 全callで固定seed
（バグ再現）、(B) call毎に変化するseed（良いrun方式）の2条件でモデルに通し、k=0のPRを比較した。

**結果（根本原因確認実験）**:

| Layer | 固定seed（バグ再現） | 変化するseed（良いrun方式） | 倍率 |
|---|---|---|---|
| Blk-0 | 1.27 | 7.93 | 6.2× |
| **Blk-4** | 1.90 | **53.71** | 28.3× |
| **Blk-9** | 2.91 | **54.23** | 18.6× |
| **Blk-13** | 4.07 | **50.04** | 12.3× |
| Blk-18 | 9.10 | 14.15 | 1.6× |
| Blk-22 | 9.23 | 11.70 | 1.3× |
| Blk-27 | 2.72 | 2.78 | ~1.0× |

**解釈**: シーン・観測は一切変えず（全く同一の352 call）、seedを変化させるだけでBlk-4/9/13の
PRは元の良いrunの値（≈53〜56）にほぼ完全復元した。これは完全な確証であり、当初検討され
否定された「キッチンレイアウト多様性」「画像ピクセル多様性」という2つの仮説とは別の、
見落とされていた3つ目の要因——初期ノイズのseed固定——が唯一かつ十分な原因だったことを示す。
層による感度の違いも意味がある: Blk-4/9/13（中間層）はseed変化に強く反応する（12〜28倍）
一方、Blk-18/22（深層寄り）は僅かにしか反応せず（1.3〜1.6倍）、Blk-27（最深層）はほぼ無反応
（1.0倍）。これは、深層は条件付け由来の情報（後述§6）に強く依存し初期ノイズの具体的な実現値には
あまり依存しない一方、中間層の表現は「どの特定のノイズを引いたか」に強く紐づいていることと
整合する。

**修正**: 該当スクリプトの`seed=cfg.seed`という行を`seed=cfg.seed + total_calls`
（call毎に変化）に修正した。この修正を受けて、崖→滑らかの判定を正しい絶対値の上で再検証する
必要が生じた。

**手法（修正版での崖→滑らか再検証）**: 修正したスクリプト（`seed=cfg.seed+total_calls`）で
25 episodesを再実行した。手違いで独立に2回（run A: N=547 calls, 成功率64.0% / run B: N=583
calls, 成功率56.0%）実行することになり、期せずして頑健性の追加確認が得られた。

**結果（修正版、σの関数としてのPR、2つの独立な再実行）**:

| Layer | idx0(σ=80) | idx2(σ=42.3) | idx4(σ=21.0) | idx5(σ=14.4) | idx6(σ=9.6) | idx7(σ=6.3) | idx8(σ=4.0) |
|---|---|---|---|---|---|---|---|
| Blk-0（run A / run B） | 6.6/8.0 | 6.4/7.8 | 6.0/7.4 | 5.8/7.1 | 5.6/6.9 | 5.5/6.8 | 5.5/6.8 |
| **Blk-4（run A / run B）** | 47.9/51.3 | 48.2/51.4 | 48.2/51.3 | 47.5/50.5 | 44.8/47.2 | 35.4/36.4 | **18.2/18.0** |
| **Blk-9（run A / run B）** | 43.5/49.3 | 44.1/50.0 | 43.7/50.0 | 42.6/48.8 | 39.8/45.5 | 31.8/35.3 | **17.7/18.2** |
| **Blk-13（run A / run B）** | 44.0/43.1 | 44.5/43.8 | 44.4/43.8 | 43.4/42.9 | 41.3/41.0 | 34.1/34.3 | **19.8/20.0** |
| Blk-18（run A / run B） | 6.6/8.0 | 6.8/8.1 | 7.0/8.4 | 7.2/8.6 | 7.5/8.9 | 7.9/9.4 | 8.6/10.0 |
| Blk-22（run A / run B） | 4.3/5.6 | 4.5/5.8 | 4.8/6.1 | 5.1/6.4 | 5.6/7.0 | 6.7/8.0 | 8.1/9.4 |
| Blk-27（run A / run B） | 2.62/2.64 | 2.63/2.66 | 2.66/2.70 | 2.69/2.75 | 2.70/2.78 | 2.68/2.77 | 2.61/2.68 |

**解釈**: seed修正が絶対値を完全に復元した——Blk-4/9/13は今度はσ=80〜21で44〜51というレベルを
維持し（§3.1の50-episode runの55.8に近い水準）、これは§4.2で確認したepisode学習曲線
（25epで≈53）とも整合する範囲である。**崖 vs 滑らかの結論**: Blk-4/9/13はidx4(σ=21.0)〜
idx8(σ=4.0)の**5ステップにわたって連続的に減少**し（例 Blk-4: 48.2→47.5→44.8→35.4→18.2）、
単一ステップの「崖」ではなく**滑らかで連続的な多段階の減少**であることが、2つの独立な修正版
再実行の両方で一貫して確認された。これは§3.1で見た「単一ステップの崖」という5-stepスケジュール
での観察が、実際には解像度不足によるものであり、真の形状は連続的な減少であることを確定させる。
Blk-0/18/22の逆パターン（Blk-0はσが小さくなるにつれ僅かに減少、Blk-18/22は明確に増加）も
確認され、§3.1の報告と定性的に一致する。

**J-bはseedバグの影響を受けていなかった**（確認）: `‖self_attn生出力‖/‖block入力(残差)‖`で
定義される指標J-bは、1回のフォワードパスで定義される比であり、複数call間の多様性尺度ではない
ため理論上seed固定の影響を受けにくいと予想された。修正版再実行の結果と、seedバグ入りの旧結果を
比較すると（Blk-18の例、idx0→idx8）: 旧「0.085→0.083→0.081→0.079→0.076→0.071→0.065」、
新「0.085→0.084→0.082→0.080→0.077→0.072→0.066」——ほぼ完全に一致しており、J-bはseedバグの
影響を受けていなかったことが確認された。

**結論**: PRのepisode数依存性は方向としては正しかったが（§4.2）、`extended_step_effrank_jb.py`
系列のスクリプトが示す極端に低いPRの真因は別にあった。真因は全callで初期ノイズのseedを固定して
いた実装バグであり、シーンを一切変えずseedのみ変化させるクリーンな実験でPRが2-4→50-54に完全
復元することを実証した上で、スクリプトを修正し、2回の独立な再実行で崖→滑らかの結論（σ=21→4に
わたる連続的な多段階減少）が正しい絶対値の上で再現されることを確認した。J-bは元々このバグの
影響を受けていなかった。

**注意点・限界**: 今後この系列のスクリプトで過去に生成された修正前のPRデータ
（絶対値・相対形状のいずれについても）は信頼できないものとして扱い、引用しないこと。
本節の教訓として、policy call毎にモデルへ渡す`seed`引数はデノイジングの初期ノイズを直接
制御するため、複数call間の多様性（PR等）を測定するスクリプトでは**必ずcall毎に異なるseedを
使うこと**を実装上のチェックリストに加えるべきである。

**今後の検証案**: 複数シードでの崖→滑らか形状の再現性確認（後述§10）。

---

## 5. テーマ4: 線形プロービングによるタスク進行度・グリッパー状態のデコード

> **要旨**: 各層の行動トークン特徴量から、タスクの進行フェーズ（progress_3）とグリッパーの
> 開閉状態（gripper_2/gripper_3）を線形分類器でどの程度デコードできるかを、全7層×全5
> デノイジングステップで測定した。**グリッパー状態は全層・全ステップで極めて高精度
> （多くの層で95%以上）にデコードでき、特に深層（Blk-18/22/27）はデノイジングがほぼ
> 純ノイズの初期ステップ（k=0）でも既に高精度である**。この「k=0でも高精度」という事実は、
> 単純な「デノイジングが進むほど情報が蓄積する」という描像とは整合せず、後述テーマ5の
> 条件付け情報伝播の検証につながる重要な手がかりとなった。タスク進行度（progress_3）は
> 深層でより高精度だが、グリッパーほど極端な精度には達しない。

### 5.1 プロービング手法

**目的**: DiTの各層の内部表現が、タスク実行に関連する意味のある情報（進行フェーズ・
グリッパー状態）をどの程度線形分離可能な形でエンコードしているかを定量化する。

**手法**: fold内PCA（Gram行列法、標準化してからPCA、n_comp=30、§4.1で確定した正しい前処理）
+ LogisticRegression（L2正則化、クラス重みバランス）を用いる。評価はLOEO（Leave-One-Episode-Out）
交差検証（50フォールド）とし、有意性は各foldでepisodeブロック単位でラベルをシャッフルする
permutationテスト（100回）で確認、複数比較（7 layers × 5 steps × 3 labels = 105テスト）は
Benjamini-Hochberg法によるFDR補正（α=0.05）で調整した。分類器は「1 policy call = 1推論
（32-stepチャンク）」を単位として学習し、入力は行動トークンの2048次元埋め込み（policy call
ごとに1ベクトル）である。ラベルは3種類用意した: `progress_3`（タスク進行度を3分割、
quantile分割でクラスバランスをほぼ均等化）、`gripper_2`（グリッパー開閉の2値、中央値を
閾値として使うことでクラス不均衡を補正）、`gripper_3`（グリッパー3クラス: 開/閉開始/閉完了）。

### 5.2 progress_3 / gripper_2 / gripper_3 の精度

**結果**（50 episodes、N=1108 calls、単一run。チャンスレベル: progress_3=33.9%、
gripper_2=50.0%、gripper_3=33.4%）:

**progress_3（タスクフェーズ3クラス）**:

| Layer | k=0 | k=1 | k=2 | k=3 | k=4 |
|---|---|---|---|---|---|
| Blk-0 | 0.43 | 0.43 | 0.47 | 0.53 | 0.55 |
| Blk-4 | 0.37 | 0.37 | 0.38 | 0.40 | 0.44 |
| Blk-9 | 0.39 | 0.39 | 0.38 | 0.42 | 0.44 |
| Blk-13 | 0.46 | 0.48 | 0.49 | 0.50 | 0.57 |
| **Blk-18** | **0.70** | 0.70 | 0.70 | 0.70 | 0.70 |
| **Blk-22** | **0.73** | 0.73 | 0.73 | 0.72 | 0.72 |
| **Blk-27** | **0.73** | 0.73 | 0.74 | 0.73 | 0.72 |

**gripper_2（グリッパー2値分類、中央値バランス後）**:

| Layer | k=0 | k=1 | k=2 | k=3 | k=4 |
|---|---|---|---|---|---|
| Blk-0 | 0.59 | 0.60 | 0.66 | 0.81 | 0.89 |
| Blk-4 | 0.56 | 0.56 | 0.68 | 0.85 | 0.91 |
| Blk-9 | 0.58 | 0.59 | 0.70 | 0.86 | 0.91 |
| Blk-13 | 0.62 | 0.65 | 0.75 | 0.87 | 0.91 |
| **Blk-18** | **0.94** | 0.94 | 0.94 | 0.95 | 0.97 |
| **Blk-22** | **0.96** | 0.96 | 0.96 | 0.97 | 0.97 |
| **Blk-27** | **0.98** | 0.98 | 0.98 | 0.98 | 0.98 |

**BH-FDR有意セル数（35テスト: 7 layers × 5 steps、各ラベル種別）**:

| ラベルタイプ | 有意セル数 | 代表精度（k=4, Blk-27） |
|---|---|---|
| progress_3 | 27/35 | 0.697 [0.641, 0.755] |
| gripper_2_phys（物理開閉基準） | 33/35 | 0.976 [0.964, 0.986] |
| gripper_2_median（中央値基準） | 33/35 | 0.968 [0.945, 0.985] |

**k依存性の詳細（gripper_2_phys, LOEO, BH-FDR補正済み）**:

| k \\ Blk | 0 | 4 | 9 | 13 | 18 | 22 | 27 |
|---|---|---|---|---|---|---|---|
| k=0（σ=80、ほぼ純ノイズ） | 0.600（n.s.） | 0.646* | 0.800* | 0.838* | **0.965*** | **0.970*** | **0.975*** |
| k=4 | 0.955* | 0.946* | 0.958* | 0.961* | 0.974* | 0.977* | 0.976* |

（*=BH-FDR有意, n.s.=非有意）

**解釈**: グリッパー状態は全層・全ステップで95%+の精度（深層でBH有意）に達し、Cosmos
Policyの表現全体に強くエンコードされていることが分かる。タスク進行度は中〜深層
（Blk-9以降）で有意だが、浅層（Blk-0, 4）ではk=4でも有意でない。深層（Blk-18/22）が
最高精度（73%）であり、Blk-27はやや低下する。kが増えるにつれて精度が微妙に改善する
傾向は特に中間層で顕著だが、深層（Blk-18/22/27）はk=0からすでに高精度でありデノイジング
ステップにほとんど依存しない。

**特に注目すべき点**: 浅層（Blk-0）はむしろchance水準に近い（k=0で0.600, 非有意）が、
深層はk=0から既に高精度（96.5〜97.5%）という、単純な「デノイジングが進むほど深層が
徐々に情報を蓄積する」という描像とは整合しないパターンが見られる。行動チャンクがまだ
ほぼ純ノイズの状態（k=0, σ=80）でもグリッパー状態を高精度に予測できるということは、
**深層はkに関わらず、常に観測条件付け（画像/proprio）にアクセスできる位置にあり、
その情報がグリッパー状態を強く予測している可能性**を示唆する——これは、単に
「デノイジングによる行動デコードが進行した」ことの証拠ではなく、条件付け経由の情報
伝播が深層で強く働いていることを示唆する。この可能性を直接検証したのがテーマ5
（§6）である。

**結論**: グリッパー状態のデコードは層によって性質が異なる——深層は条件付け経由で
早期（k=0）から高精度、浅層はデノイジングの進行とともに精度が上がる。タスク進行度は
深層でより高精度だが、グリッパーほど極端な精度には達しない。「グリッパー状態は
Cosmos Policyの表現全体に強くエンコードされている」という総括だけでは、この
層依存性（浅層はkに強く依存、深層はkに依存しにくい）を無視した過度の一般化になる。

**注意点・限界**: 本節の精度はfold内PCA+LogisticRegressionという特定のパイプライン
（§4.1で確定した正しい前処理）に基づく。この高精度なグリッパーデコードが「行動潜在の
情報を読んでいる」のか「条件付け情報を読んでいる」のかは、本節の解析だけでは切り分け
られない（後述§6.2, §6.3で直接検証する）。

**今後の検証案**: 条件付け情報の因果的な寄与を切り分ける統制実験（§6.2, §6.3）。
複数シードでの再現性確認（§10）。

---

## 6. テーマ5: 条件付け情報（画像・proprio）の伝播経路

> **要旨**: テーマ4で見た「深層のグリッパープローブがk=0（ほぼ純ノイズの入力）でも
> 高精度」という現象を手がかりに、条件付け情報（画像・proprio）が自己注意を通じて
> どのように行動トークンに伝播しているかを解析した。行正規化した自己注意重みの解析
> （成功率0%の探索的runと成功率58%の確証的runの両方）により、**Blk-18が特異的に
> proprioへの注意を強めるサージを示す**ことを発見し、これは固定入力統制実験（T8）での
> proprio差し替え感度が最も高い層とも一致した。さらに、より直接的な情報源テスト（T8b、
> gripper状態が異なる2つの観測の条件付けを入れ替えたときプローブ予測が追従するか）に
> より、**単独チャネル（proprioのみ・画像のみ）の差し替えではプローブは元の予測に
> 留まるが、画像+proprioを同時に差し替えると、深さに応じて単調にsourceへの追従が
> 強まる（最深層で85%）**ことを確認した。4つの独立解析（自己注意・T8・T8b・J-b）
> がいずれもBlk-18を条件付け感度の観点で特異的な層として指し示している。

### 6.1 自己注意解析（行正規化Tマトリクス）

**目的**: 行動トークン（T=5）が、潜在系列中の他のどのトークン位置（proprio・各カメラ
画像・自分自身など）に注意を向けているかを定量化し、「モダリティ対応型注意」（例えば
行動トークンがproprioに強く注意する）の有無と層依存性を確認する。

**手法**: 各(block, k)ごとにquery×keyのsoftmax注意重みを集計し、行正規化（各行を行和で
割り、行和=1に変換）してtoken-type間の注意分布とした。一様分布（全T位置に均等に注意する
場合）を帰無仮説として `1/STATE_T = 1/11 ≈ 0.0909` をヌルとし、観測割合との比を効果量とした。

**結果**（確証的run: N=50 episodes, 成功率58%, 1128 policy calls、T1/T2サニティ再確認済み。
行動トークン(T=5)→各タイプ、行正規化、中間層Blk-9,13,18,22平均, k=4）:

| 入力/出力タイプ | 観測割合 | 一様null | 効果量 |
|---|---|---|---|
| **action（自己参照）** | 0.4153 | 0.0909 | +3.57×（4.57×） |
| **proprio (T=1)** | 0.1150 | 0.0909 | +0.27×（1.26×） |
| curr_secondary (T=4) | 0.1060 | 0.0909 | +0.17×（1.17×） |
| curr_primary (T=3) | 0.0336 | 0.0909 | −0.630× |
| blank (T=0) | 0.0219 | 0.0909 | −0.759× |

**層別プロファイル（k=4: 行動トークンの行正規化注意）**:

| Block | →self (action) | →proprio | →curr_wrist | →curr_primary | →curr_secondary |
|---|---|---|---|---|---|
| Blk-0 | 0.092 ≈unif | 0.089 ≈unif | 0.091 ≈unif | 0.092 ≈unif | 0.092 ≈unif |
| Blk-4 | 0.385 (4.2×) | 0.059 | 0.019 | 0.018 | 0.073 |
| Blk-9 | 0.560 (6.2×) | 0.052 | 0.037 | 0.032 | 0.131 |
| Blk-13 | 0.446 (4.9×) | 0.077 | 0.079 | 0.037 | 0.145 |
| **Blk-18** | 0.265 (2.9×) | **0.202 (2.2×)** | 0.102 | 0.037 | 0.071 |
| Blk-22 | 0.391 (4.3×) | 0.129 | 0.070 | 0.029 | 0.077 |
| Blk-27 | 0.449 (4.9×) | 0.081 | 0.016 | 0.011 | 0.016 |
| 一様null | 0.091 | 0.091 | 0.091 | 0.091 | 0.091 |

**解釈**: Blk-0は全タイプほぼ一様であり、最初の層では注意の選好は存在しない。
Blk-4,9,13は強い自己参照（4〜6倍の一様分布）とcurr_secondaryへの中程度の上昇を示し、
行動トークンが中間層で自己情報を統合している様子を示す。**Blk-18ではproprioへの
注意が突出して増加（+2.2倍の一様分布）し、自己参照が相対的に後退する**——固有感覚
情報を積極的に参照する特異的な深さである。Blk-22, 27では自己参照が再び支配的になる。

**補足（探索的run、成功率0%、N=1600 calls）**: 別途、より探索的な条件（成功率0%、
モデルロード時のデバイス設定に起因すると考えられる非決定性の影響を受けたrunだが、
データ収集自体は正常に完了している）で行った先行解析でも、質的に一致するパターンが
確認されている。11×11の全T位置間の注意行列を追跡したところ、層の深さに応じた
「モダリティ対応の成熟過程」が見られた: Block-0（均一・特化なし）→Block-4（自己注意の
確立）→Block-9（future_wrist→curr_wrist等、モダリティ対応がTop-2に登場）→Block-13
（モダリティ対応の強化）→Block-18（future_proprio→proprioが最上位になるなど
モダリティ対応が最も明確）→Block-27（blankトークンが情報ハブとして台頭）。
またAttention Rollout（残差接続を考慮した累積注意、`R[b]=A_hat[b]@R[b_prev]`で
ブロック順に累積）では、全出力トークンで自己参照が97.5〜97.9%と圧倒的であり
（残差接続による恒等伝播が支配的であることの反映であり、単層の注意行列で見える
モダリティ対応が「使われていない」ことを意味するわけではない）、rollout上でも
proprio→action系列のクロス項が層が深まるにつれて増加する傾向が確認された
（Block-0で0.05%→Block-27で0.35%）。また、注意パターンはデノイジングステップ
（k=0〜4）を通じてほぼ完全に不変であった（最大差分は全7ブロックで0.001程度）
——「どこを見るか」はσに依存せず一貫している。

**結論**: **Blk-18が自己注意でproprioへの特異的なサージを示す**ことは、確証的な
N=1128 callのrunで頑健に確認された。この所見は探索的な先行run（N=1600 call）の
質的パターンとも整合する。注意パターン自体はデノイジングステップに依存せず、
どのブロックがどこに注意を向けるかは主に層の深さで決まる。

**注意点・限界**: 補足で述べた探索的runは成功率0%というロールアウトの質に懸念がある
条件で収集されたデータであり、定量的な数値（Top-1/2/3の具体的な重み値など）は
確証的runの値と直接比較すべきではない。あくまで定性的なパターン（モダリティ対応の
段階的成熟、自己参照の支配性、デノイジング不変性）の参考情報として扱う。

**今後の検証案**: 複数シードでの再現性確認（§10）。

### 6.2 T8: 固定入力コントロール

**目的**: §5.2で見た「深層のグリッパープローブがk=0でも高精度」という所見は、「デノイジング
による行動デコードの進行」ではなく「画像・proprio条件付けが自己注意を通じて行動トークンに
伝播している」ことで少なくとも部分的に説明できるのではないか、という仮説を直接検証する
統制実験。単なる相関ではなく、条件付けを実際に**差し替えて**変化が生じるかを見ることで
因果的な寄与を確認する。

**手法**: 同一の行動トークン特徴量抽出パイプラインを用い、1回のリセットで得た観測
`obs_base`に対して以下の4系列を**同一シード・同一タスク記述**で比較した: `baseline1`/
`baseline2`（`obs_base`を全く同じ入力で2回実行、決定性の床を確認する対照群）、
`img_swap`（`primary_image`のみ別エピソードの画像に差し替え）、`proprio_swap`
（`proprio`のみ別エピソードの値に差し替え）、`all_swap`（画像+proprioを全て別エピソードの
値に差し替え）。N=30ペアで実行、5-stepデノイジング。

**結果**: 決定性フロア（`baseline1` vs `baseline2`）は全(layer, k)で厳密に0.000
（完全な決定性を確認）。

**k=0（σ=80、行動潜在はほぼ純ノイズ）での相対‖Δfeat‖（N=30ペア平均）**:

| 変異 | Blk-0 | Blk-4 | Blk-9 | Blk-13 | Blk-18 | Blk-22 | Blk-27 |
|---|---|---|---|---|---|---|---|
| img_swap | 0.023 | 0.020 | 0.023 | 0.036 | **0.059** | 0.040 | 0.015 |
| proprio_swap | 0.009 | 0.010 | 0.022 | 0.045 | **0.082** | 0.070 | 0.022 |
| all_swap | 0.063 | 0.056 | 0.065 | 0.109 | **0.202** | 0.158 | 0.051 |

k=4（最終ステップ）でもほぼ同水準（Blk-18: img=0.046, proprio=0.063, all=0.158）——
Δの大きさはk=0とk=4でほぼ変わらない。

**行動出力レベル（gripper次元、baseline1との平均絶対差）**:

| 変異 | gripper次元 Δ（絶対） | 全次元 L2 Δ |
|---|---|---|
| img_swap | 0.0011 | 0.074 |
| proprio_swap | 0.0015 | 0.085 |
| all_swap | 0.0018 | 0.310 |

**解釈**: 画像・proprioいずれの差し替えも、行動トークンの内部表現を決定性フロア（0.000）に
対して明確に非ゼロなΔで変化させる。**Δはk=0（純ノイズ入力）でもk=4（最終ステップ）と
ほぼ同じ大きさ**であり、これは深層の行動トークン表現がデノイジングの進行とは独立に、
常に条件付け情報にアクセスしその内容を反映していることを示す。**Blk-18が最も感度が高く**
（proprio_swapでΔ=0.082 @ k=0）、これは§6.1の自己注意解析でBlk-18が唯一proprioへの
注意を強めるサージを示した層と一致する——独立に測定した2つの解析（アテンションパターンと
条件付け感度）が同じ層を指し示している。

一方、**内部特徴量（feature-Δ最大0.20）の変化に対し、gripper出力自体の変化はわずか
（Δ=0.0018）**であり、この非対称性には注意が必要である。条件付けの差し替えが内部表現を
動かすことは示せたが、この事実だけからは「gripperプローブの高精度が条件付け由来で
説明できる」とまでは言い切れない——内部特徴が動いても、プローブが実際に読み取っている
情報がその変化した部分なのかどうかは、この統制実験の測定対象（feature-Δ、output-Δ）
だけでは分からない。この限界に直接応えるのが§6.3のT8bである。

**結論**: 条件付け（画像・proprio）は行動トークンの内部表現にk=0から到達しており、
深層のBlk-18で特に感度が高い。ただし、これが線形プローブの高精度な予測の情報源に
なっているかどうかは、本節の実験だけでは未確定であり、より直接的な情報源テスト
（§6.3）が必要である。

**注意点・限界**: N=30ペアという小規模なサンプルであり、また本節の測定（feature-Δ,
output-Δ）は「条件付けが内部表現に影響する」ことを示すのみで、「プローブの予測根拠が
条件付け由来か」という問いには直接答えていない。

**今後の検証案**: §6.3で実施した情報源テストがこの限界に対応する。

### 6.3 T8b: gripperプローブの情報源テスト

**目的**: §6.2のT8では、条件付けの差し替えが内部特徴量を動かすことは示せたが、
gripper出力自体の変化はわずかであり、「gripperプローブが条件付けを読んでいるか」を
実は直接検証していなかった。この限界に応えるため、「gripper状態が異なる2つの観測の
条件付けを入れ替えたとき、学習済みgripperプローブの予測が差し替え先（source）を
追従するか、元（base）のままか」を直接測定する。

**手法**: 通常のclosed-loopロールアウト（自身の行動で環境を実際に進める、15 episodes）を
行い、各policy callで(a)実際にモデルに入力された観測、(b)行動トークン特徴量（k=0、
baseline/未差し替え）、(c)ラベルをキャッシュした（N=311〜360 calls）。ラベルが異なる
2つのcall（i, j、異なるepisode由来）のペアを全て抽出し、call iの条件付け（proprio単独/
画像単独/全部）をcall jのもので差し替えて（iの乱数シードは固定）再度モデルに通し、
行動トークン特徴量を取得。**iとjの両方のepisodeのデータを除外して学習したプローブ**で
この差し替え特徴量を分類し、予測がjの真のラベル（follows_source、条件付け由来）に
一致するかiの真のラベル（stays_base、行動潜在由来）に一致するかを判定した。ラベル定義は
2種類試した: (1) `current_proprio`（観測時点の生のproprioの中央値2値化）、(2)
`action_gripper`（最終デノイジングステップでモデルが実際に出力したgripper次元の
32-stepチャンク平均の中央値2値化——これは既存のgripperプローブが実際に予測していた
ラベルと同じ定義）。

**結果(1): current_proprioラベルは深層でも弱くしか予測できない——「単純な生proprio
通過」説の否定**:

| Layer | LOEO acc (current_proprio, k=0) |
|---|---|
| Blk-0 | 0.494 |
| Blk-18 | 0.555 |
| Blk-22 | 0.607 |
| Blk-27 | 0.653 |

既存のaction_gripperベースのプローブ（k=0）ではBlk-18/22/27が96.5〜97.5%だったのに対し、
「現在の生proprioそのもの」を予測しようとすると深層でも55〜65%程度にしか到達しない。
これは、深層の高精度gripperプローブが「入力proprioの単純な通過（パススルー）」では
説明できないことを示す反証である——深層が学習しているのは、現在のproprioそのもの
ではなく、より複雑な（条件付け由来かもしれない）何かである。

**結果(2): action_gripperラベル（元のプローブと同じ定義）でのswap追従テスト**

**baseline LOEO acc（action_gripper, k=0, N=360, 15 episodes）**:

| Layer | LOEO acc | 参考: 全データ（N=1108, 50ep）でのacc |
|---|---|---|
| Blk-0 | 0.564 | 0.600 (n.s.) |
| Blk-9 | 0.752 | 0.800* |
| Blk-13 | 0.779 | 0.838* |
| **Blk-18** | **0.747** | 0.965* |
| **Blk-22** | **0.775** | 0.970* |
| Blk-27 | 0.891 | 0.975* |

小規模プール（N=360）でもBlk-9/13は元の値に近いが、**Blk-18/22は元の96.5〜97.0%に対し
74.7〜77.5%と20pp以上低い**。この精度差の原因を追加検証したところ（良い`features.npz`から
実際に15エピソード相当をランダムに20回再抽出し、同一手法で精度を再計算）、**サンプルサイズ
の違いだけでは説明できない**ことが判明した:

| Layer | 15ep再抽出（20試行, 平均±std） | 15ep再抽出の最小値 | T8bの実測値 | 全データ（N=1108, 50ep） |
|---|---|---|---|---|
| Blk-18 | 0.921±0.030 | 0.863 | **0.747** | 0.948 |
| Blk-22 | 0.955±0.023 | 0.902 | **0.775** | 0.957 |
| Blk-27 | 0.966±0.023 | 0.923 | 0.891 | 0.969 |

良いデータを同じ規模（15episodes）に縮約しても、Blk-18/22の精度は20回の試行全てで86%以上に
留まり、T8bが実際に示した74.7〜77.5%には一度も届かなかった——サンプルサイズだけでは説明
できない、統計的に有意な追加のギャップがT8bのプールに存在することが確定した。追加で
キッチンレイアウト・言語条件付け（task description）の多様性という2つの仮説も検証したが、
いずれも否定された（単一env+繰り返しresetパターンで実際にtask descriptionがepisode毎に
異なる物体名を含めて正しく変化していることを確認した）。**T8bの深層プローブ精度低下の
真因は、本セッションでは特定できなかった**（サンプルサイズ・レイアウト・言語条件付けの
いずれでもないことは確定した）。ただし、self-check精度（iとjのepisodeを除外して学習した
プローブでbase自身のラベルを予測、Blk-18=0.75, Blk-22=0.78, Blk-27=0.92）は、以下のswap
テストの土台として十分な精度がある（絶対精度が最適でなくとも、follows_source/stays_base
の相対比較という本テストの目的には影響しない）。

**swap追従率（N=60 discriminating pairs、follows_source = 差し替え先(source)の真のラベルに
予測が一致した割合）**:

| Layer | proprio_swap | img_swap | all_swap |
|---|---|---|---|
| Blk-0 | 0.417 | 0.367 | 0.517 |
| Blk-9 | 0.583 | 0.217 | **0.683** |
| Blk-13 | 0.417 | 0.250 | **0.667** |
| **Blk-18** | 0.283 | 0.267 | **0.733** |
| **Blk-22** | 0.383 | 0.217 | **0.767** |
| **Blk-27** | 0.283 | 0.117 | **0.850** |

**解釈**: 単独チャネルの差し替え（proprio_swapまたはimg_swapのみ）は、いずれの層でも
プローブの予測を元（base）に留める方向に働く（follows_source率はほぼ全て0.5未満、
深層ではimg_swap単独で0.12〜0.27まで低下）。これは、proprio単独/画像単独の「部分的な」
条件付け差し替えは、モデルにとって一貫性のない（どちらのepisodeにも完全には対応しない）
入力になり、むしろ元の行動潜在に留まりやすいことを示唆する。**しかし全条件付け（画像+
proprio）を同時に差し替えると（all_swap）、深さに応じて明確に単調増加する形でsourceへの
追従が強まる**（Blk-9:0.68→Blk-13:0.67→Blk-18:0.73→Blk-22:0.77→Blk-27:0.85）。これは、
**深層のgripperデコードは「画像+proprioが一貫した（自己整合的な）条件付けとして揃って
差し替えられた場合」に、条件付け由来の情報へ明確に追従する**ことを示す直接証拠である。
self-check精度（0.75〜0.92）を踏まえると、この結果はノイズでは説明できない。

**結論**: §6.2のT8で示唆された「gripper 96-97%は条件付け経由で少なくとも部分的に説明
できる」という仮説は、本テストにより**単独チャネルでは支持されないが、画像+proprioの
結合条件付けとしては支持される**、とより正確に述べ直す必要がある。また、深さに応じた
単調な追従率の増加は、Blk-18が自己注意サージ・T8のproprio感度・J-bで示した「条件付けへの
特異的な感度」と一貫しており、深層ほど条件付けの結合表現への依存が強まるという描像を
強化する。

**注意点・限界**: N=360（15 episodes）は元の確立されたプローブ（N=1108, 50 episodes）
より小さく、Blk-18/22のbaseline精度が20pp低い。この精度差はサンプルサイズだけでは
説明できないことが確定しており、レイアウト・言語条件付けの多様性も原因ではないことを
確認したが、**真因は本セッションでは特定できておらず、未解決の限界**として記録する
（§10）。

**今後の検証案**: より大規模なpoolでの再現、およびT8bのロールアウト自体の成功率・
行動品質の確認。

### 6.4 Blk-18三角測量の総合評価

**目的**: §6.1〜6.3で個別に得られた「Blk-18が条件付けに対して特異的な感度を示す」という
所見が、複数の独立した解析にわたって一貫しているかを総合評価する。

**手法**: 4つの独立解析——自己注意の行正規化Tマトリクス（§6.1）、T8のproprio_swap感度
（§6.2）、T8bのall_swap追従率（§6.3）、および J-b（`‖self_attn生出力‖/‖block入力(残差)‖`、
§4.3で修正・再検証済み）——のそれぞれについて、Blk-18が他層と比べてどの程度際立つ結果を
示すかを横断的に確認した。

**結果**:
- **自己注意**（§6.1、N=1128 call run）: Blk-18が唯一proprioへの注意サージを示す
  （一様の2.2倍）。
- **T8**（§6.2、N=30 pairs）: Blk-18がproprio_swap感度で最も高い値を示す（Δ=0.082 @ k=0）。
- **T8b**（§6.3、N=60 discriminating pairs）: Blk-18はall_swap追従率が深さについて単調増加
  する系列の中でBlk-27に次ぐ高さ（0.733）を示す——4つ目の独立した一致。
- **J-b**（§4.3で修正済み）: Blk-18がσに対して単調減少する唯一の層（0.085→0.065、2つの
  独立な修正版再実行でも0.085→0.066と再現）。

**解釈**: 4つの独立した解析（自己注意、T8、T8b、J-bの形状）がいずれもBlk-18を条件付け
感度の観点で特異的な層として指し示しており、単一のrunの偶然ではなく、複数の異なる観点
からの一致として妥当性が高まったと判断する。

**結論**: Blk-18は、深層側の中でも特に「条件付け（特にproprio）への感度が強く、かつ
デノイジングが進むにつれてその感度パターンが変化する」層として、複数の独立解析から
一貫して特定された。

**注意点・限界**: これら4つの解析は、それぞれ別々の単一実行（single run: 自己注意
N=1128 call、T8 N=30 pairs、T8b N=60 pairs、J-b N=547/583 calls）に基づく。単一runの
中では頑健な一致だが、**multi-seedでの再現確認は行っていない**——これは今後の課題として
明記する。また「プローブ層順位」（深層が中間層より高精度、等）についても、テーマ3で
判明したPRの絶対値のepisode数依存性がJ-b・PR双方の絶対値解釈に影響しうるため、
layer間の絶対的な精度の順位付けよりも、質的なパターン（深層で条件付け由来の情報が
強い）を主張の中心に置く方針を採る。

**今後の検証案**: 複数シードでの独立な再現実験により、Blk-18三角測量がrunをまたいで
頑健かを確認することが最優先の今後の課題である（§10）。

---

## 7. テーマ6: 言語クロスアテンションとpadトークン

> **要旨**: Cosmos PolicyはT5言語埋め込み（実トークン数に対し512トークンまでpaddingされる）
> に対してcross-attentionを行うが、モデルはこのcross-attentionにpaddingマスクを適用しない
> ことが判明した——実トークンへの注意重みはわずか2〜7%程度で、残り93〜98%はpadトークンに
> 「乗る」。一見するとモデルの欠陥のように見えるこの現象を、コード調査・チェックポイント
> 調査・実データでの直接数値照合という3段階の検証で掘り下げたところ、**pad位置のK・Vは
> アーキテクチャ上（T5埋め込みのゼロパディング＋バイアス無し線形層という設計により）厳密に
> ゼロになるため、softmax確率質量のほとんどがpadに乗っても出力には数学的に全く影響しない**
> ことを直接照合で確認した。「学習によって抑制されている」という当初の推測は誤りで、
> 正しくは「アーキテクチャ上の必然」である。

### 7.1 クロスアテンションの分布とpadトークンへの重み集中

**目的**: 行動トークン（および他の生成対象トークン）が、言語条件付けトークン（T5埋め込み、
実トークン数15個+pad497個=512トークン）のどこに注意を向けているかを定量化し、モデルが
padding部分をどう扱っているかを確認する。

**手法（探索的解析、成功率0%のrun）**: 先行の探索的解析では、50エピソード・1600 policy call
（**この解析の成功率は0%であり、CUDAデバイス設定に起因する非決定的挙動の可能性がある。
データ収集自体は正常に完了しているが、ロールアウトの質そのものには疑念が残ることに
注意が必要である**）で、各(block, k)ペアの512トークン全体への注意重みを収集した。
実トークン数n_real=15、pad=497、一様分布ヌルはH_uniform=log(15)=2.708 nats,
uniform_top1=1/15=0.0667とした。

**結果（探索的解析、層別Top-3注目トークンの例、k=4）**:

| 層 | Top-1 | Top-2 | Top-3 | H_real（実トークン内エントロピー、最大2.71） |
|---|---|---|---|---|
| Block-0 | "the" | "bottled" | "the" | 2.680（最も分散） |
| Block-9 | "bottled" | "drink" | "the" | 2.438 |
| Block-18 | ""（語頭subword） | "bottled" | "the" | 2.537 |
| Block-27 | "the" | "cabinet" | "</s>" | 2.037（最も集中） |

デノイジング不変性の定量（k=0とk=4の注意行列の最大差）は全層で0.0000015〜0.0010程度と
極めて小さく、注目トークンはデノイジングステップを通じてほぼ不変であった。全層で
H_all≈6.23（512トークン一様分布の最大値6.24に近い）——注意が512全トークンに広く分散して
いることを意味する。中間層（Block-4,9,13）はオブジェクト名（"bottled", "drink"）に最も
集中し、最深層（Block-27）は構造語・目的地（"the", "cabinet"）への集中を示す——最深部
では対象物より場所・文法を参照する。

**手法（確証的解析: pad重みの検証）**: 上記の探索的解析で収集した35 (layer,k) ペア
（各1600×512）に対し、行和が1になっているか、pad位置（15〜511）への重み合計がどの程度に
なっているかを確認した。

**結果（pad重みの検証）**:

| 項目 | 値 |
|---|---|
| 行和（unmasked, 全35ペア） | max\|sum−1\|=0（正常） |
| Pad重み（unmasked, max） | **0.0711**（全35ペア中） |
| 実トークン(0-14)への重み合計 | **4.0%**（unmasked平均） |
| Padトークン(15-511)への重み合計 | **96.0%**（497 positions） |

**この検証で発覚した実装バグ**: 当初、この結果を「モデルがcross-attentionでマスクを
適用していない」ことの証拠として報告しようとしたが、実際にはフック側の実装に問題が
あった——フックが`compute_qkv()`からQ,Kを取り出して`softmax(QKᵀ/√d)`を再計算する際、
attention_maskを適用し忘れていたのである（詳細は§11のバグ一覧を参照）。既存の生データに
`token_attention_mask`を事後適用し、実15トークン上で再正規化した版で再解析を行った。

**マスク適用後の解析（設計方針: アテンションはpadマスク後に正規化し、一様ヌルとの差で
語る）——一様ヌルとの効果量 KL(attn‖uniform)**:

| Layer | KL（k平均） | Top-1比（k=4） | 有意（全kでp<0.05） |
|---|---|---|---|
| 0 | 0.099 | 2.06× | × |
| 4 | 0.266 | 3.12× | × |
| **9** | **0.572** | **5.78×** | **✓** |
| 13 | 0.119 | 2.70× | × |
| 18 | 0.380 | 3.51× | × |
| 22 | 0.255 | 3.96× | × |
| **27** | **1.186** | **9.81×** | **✓** |

（Bootstrap CI: call単位でn=2000。Permutation検定: 全kでKL>0が有意なのはLayer 9, 27のみ）

**解釈**: モデルは実際にcross-attentionでpadding maskを適用しない設計になっている
（コード調査で`attn_mask_type="no_mask"`という明示的な設定を確認、後述§7.2）。マスク
適用後に実15トークン上で再正規化した解析では、Layer 9, 27でのみ全デノイジングステップで
一様分布と有意差があり（トークン選好あり）、Layer 0, 4, 13, 18, 22では全kで有意差がない
（「選択的注意」の主張は本データでは支持されない）。Layer 27（最終ブロック）が最も選択的
（KL=1.19、Top-1は一様の9.8倍）である。

**結論**: モデルはcross-attentionにおいて512トークン全体への無条件softmaxを計算しており、
実トークンへの重みはわずか2〜7%程度で残りはpadに乗る。padマスク後に再正規化した場合、
実トークン間の選択的な注意はLayer 9, 27でのみ統計的に確認できる。「なぜ実トークンへの
重みがこれほど小さくても機能するのか」については§7.2で直接検証する。

**注意点・限界**: 本節の探索的解析（成功率0%）は、質的なトークンレベルの所見
（"bottled"/"drink"への集中、"the"/"cabinet"への集中等）についての参考情報として扱い、
定量的な精度の主張の根拠としては用いない。padマスク後の解析（KL、Top-1比）自体は
既存の生アテンション行列に対する事後的な数学的処理であり、ロールアウトの成功率に
直接依存する量ではないため、この部分の結論は相対的に頑健と考えられる。

**今後の検証案**: 成功率の正常なrunでのクロスアテンション解析の再実施。

### 7.2 pad由来出力寄与のゼロ照合

**目的**: §7.1で見た「実トークンへの重みはわずか3%程度で残り97%がpadに乗る」という
observationに対し、それでも高い成功率を達成できる理由が、(a) pad位置のvalueがほぼ
ゼロで実質的に効いていないためなのか、(b) フックが取り逃した何らかの隠れマスクが
存在するためなのかを、直接の数値照合によって確定する。

**手法（コード調査→チェックポイント検証→実データ数値照合の3段階）**:

1. **コード調査**: DiTのAttentionクラスを確認すると、cross-attentionバックエンドが
   明示的に`attn_mask_type="no_mask"`（マスクなし）に設定されている（フックが見落とした
   隠れマスクは存在しないことのコード上の確認）。さらにT5埋め込み計算コードは実配列長を
   超える位置を`encoded_text[b][lengths[b]:] = 0`で明示的にゼロ埋めしてからキャッシュしており、
   かつk_proj/v_projは`bias=False`（RMSNorm(0)=0なのでk_normを通してもゼロのまま）。
   これは数式上、**pad位置のK・Vが厳密に0になる**ことを意味する。
2. **実データでの直接確認**: 実際に使用中のT5埋め込みキャッシュを直接ロードし、pad位置
   （実トークン数を超える範囲）の埋め込みノルムを確認したところ、**厳密に0.0**（サンプル
   例: 13トークンの実文で、14番目以降の512-13=499位置全てノルム0.0）。
3. **チェックポイントでの直接確認**: 実際にロードされる学習済みチェックポイントの
   state_dictキーを確認したところ、`cross_attn.{q,k,v,output}_proj`に`.bias`パラメータは
   一切存在しない（コード上の`bias=False`が実チェックポイントにも反映されていることを確認）。
4. **フック再構成attention·Vとモデル実出力の直接数値照合**: 7 probe layers × 5 denoising
   steps × 15 calls（N=525 records）で、cross_attnの`compute_qkv`からQ,K,Vを捕捉し、
   マスクなしsoftmaxの実際の計算ノードにforward hookを張って**モデルが実際に計算した生の
   attn·V出力**を直接捕捉。同じQ,K,Vから手動で`softmax(QKᵀ/√d)·V`を再構成し、実出力側と
   数値照合。さらに出力をreal-token由来項とpad-token由来項に線形分解して両者のノルムを
   個別に測定した。

**結果**: フック再構成とモデル実出力の一致度は `max|reconstruction−real_output| =
4.89×10⁻⁴`。この値はサニティテストT1/T2で用いた合否基準（1e-4）の**約4.9倍**であり
「ほぼ同水準」と表現するのは不正確だが、512キー（実トークン+パッド）×head_dim次元に
わたる累積和（`softmax(QKᵀ/√d)·V`）のbf16精度での丸め誤差として妥当な範囲であり、
隠れマスクの存在を示唆するものではない（隠れマスクがあれば、pad位置への寄与が体系的に
0からズレるはずだが、実測では全層で厳密に0.000000だった）。

**pad由来の出力寄与（層別、N=525 records）**:

| Layer | 実トークンへの重み | padへの重み | 出力ノルム（real由来項） | 出力ノルム（pad由来項） |
|---|---|---|---|---|
| Blk-0 | 0.027 | 0.973 | 0.0118 | **0.000000** |
| Blk-4 | 0.040 | 0.960 | 0.0183 | **0.000000** |
| Blk-9 | 0.025 | 0.975 | 0.0079 | **0.000000** |
| Blk-13 | 0.022 | 0.978 | 0.0106 | **0.000000** |
| Blk-18 | 0.065 | 0.935 | 0.0458 | **0.000000** |
| Blk-22 | 0.038 | 0.962 | 0.0270 | **0.000000** |
| Blk-27 | 0.064 | 0.936 | 0.4580 | **0.000000** |

全層で pad 由来の出力ノルムは厳密に 0.000000（丸め誤差の範囲内ですらなく、文字通りゼロ）。
実トークンへの注意重みは全層で2〜7%程度に過ぎないが、**出力の100%は実トークン由来項で
説明できる**。

**解釈**: 懸念(a)「pad位置のvalueがほぼゼロで効いていない」は確認された（正確には
「ほぼゼロ」ではなく厳密にゼロ）。T5埋め込みがpad位置を明示的にゼロ埋めし、v_projが
bias無し線形層であるため、V_padは数学的に厳密に0になる。これは学習で獲得された性質
（「学習によって抑制されている」という当初の推測）ではなく、**アーキテクチャ上の設計
（T5埋め込みのゼロパディング＋bias無し線形層）による必然**である。懸念(b)「フックが
見落とした隠れマスクがある」は否定された——`attn_mask_type="no_mask"`という明示的な
アーキテクチャ設定に加え、フック再構成が実モデル出力とbf16精度で一致することを実データで
確認した。

**結論**: 「モデルはcross-attentionでマスクを適用しない」という観察自体は事実として
正しいが、それでも動作する理由は「学習によってpad寄与が抑制されているため」ではなく、
**「pad位置のK・Vはアーキテクチャ上厳密にゼロになるため、softmax確率質量のほとんどが
padに乗っても出力には数学的に全く影響しない」**である。これはトレーニング結果に依存
しない、モデル構造から導かれる保証である。一見strikingに見えた「97%がpadに乗る」という
観測は、cross-attention出力の健全性に対する脅威ではなく、単に無駄な（しかし無害な）
softmax確率質量の分配であったと結論づけられる。

**注意点・限界**: この結論は本モデルのアーキテクチャ（T5埋め込みのゼロパディング、
bias無し線形層）に固有のものであり、異なる実装（例えばbias付き線形層を使うモデル）
では成り立たない可能性がある。

**今後の検証案**: 特になし（本解析でこの問いには十分な確証が得られている）。

---

## 8. テーマ7: 画像ラテント（将来フレーム）生成の解析

> **要旨**: これまでのテーマ1〜6は行動トークン（T=5）に焦点を当ててきたが、Cosmos Policy
> は同時に将来カメラ画像のラテント表現（future_wrist, future_primary, future_secondary）
> も生成している。本テーマでは、行動生成で確認された「Confidence-to-Commitment」的な
> パターン（デノイジング終盤ほど変化量が大きい）や層間表現類似度の崩壊が、画像ラテントの
> 生成にも共通して現れるかを検証した。**変化量の単調増加パターンは画像ラテントにも
> 普遍的に見られ、むしろ将来画像（特にfuture_wrist）は行動より大きな変化量を示す**。
> また行動トークンと将来画像トークンの層間CKAは、浅層・中間層でk=0では高い類似度を
> 持つが（ノイズが支配的な段階では全T位置の表現が同質）、k=3→4で急崩壊し、生成対象
> ごとの表現が分化することが分かった。**本テーマの解析は2026-06-30の1回のみ実施されており、
> テーマ1〜6で確立したbootstrap CI・permutationテスト・BH-FDR補正などの統計的厳密性を
> 適用していない探索的な性格のものである**ことに注意が必要である。

### 8.1 Δx̂₀ 変化量とラテントノルム

**目的**: 行動生成（テーマ1、§2.1）で確認された変化量の単調増加パターンが、同時生成される
将来画像ラテント（T=7,8,9）にも普遍的に現れるかを検証し、アクションと画像で変化量の
大きさや比率がどう異なるかを定量化する。

**手法**: フックで全5デノイジングステップのx̂₀をキャプチャし、各T位置の連続ステップ間L2差を
集計した。curr_primary (T=3、現在の画像、生成対象ではなく固定入力) はゼロ変化が期待される
対照群として使用した。50エピソード分のデータを用いた（成功率は本解析では記録されていない）。

**結果: T位置別 Δx̂₀**:

| 位置 | k=0→1 | k=1→2 | k=2→3 | k=3→4 | Δk4/Δk1 比 |
|---|---|---|---|---|---|
| future_wrist (T=7) | 1.405 | 2.195 | 3.566 | **5.143** | 3.66× |
| future_secondary (T=9) | 1.116 | 1.393 | 1.822 | **2.432** | 2.18× |
| future_primary (T=8) | 1.135 | 1.435 | 1.951 | **2.701** | 2.38× |
| action (T=5) | 0.927 | 1.178 | 1.616 | **2.103** | 2.27× |
| curr_primary (T=3、固定対照群) | 0.000 | 0.000 | 0.000 | 0.000 | — |

行動トークンの層別隠れ状態変化量（§2.1の実測値そのもの、ここでは対比のため再掲）でも
全7プローブ層で単調増加が確認されている。future_primary(T=8)の層別隠れ状態変化量についても、
全7プローブ層で同様の単調増加パターンが確認された（Block-27が最大の絶対変化量を示す点も
行動トークンと共通）。

**結果: ラテントノルムの推移**:

| 位置 | T | k=0 ノルム | k=4 ノルム | Δk=0→k=4 |
|---|---|---|---|---|
| action | T=5 | 48.73±4.66 | 49.16±4.51 | +0.9% |
| future_primary | T=8 | 97.61±13.22 | 97.84±13.25 | +0.2% |
| future_wrist | T=7 | 106.33±14.95 | 106.93±15.32 | +0.6% |
| future_secondary | T=9 | 99.75±15.23 | 99.88±15.25 | +0.1% |
| curr_primary（固定） | T=3 | 96.39±13.09 | 96.39±13.09 | 0% |

**解釈**: 全T位置で変化量の単調増加パターンが確認され、行動生成で見られた形状的パターン
（§2.1）は画像ラテントにも共通する。future_wrist (T=7) が最大変化量（k=3→4で5.143）かつ
最大増加比（3.66×）を示し、action (T=5) の変化量（2.103）は将来画像（2.4〜5.1）の
半分以下——**画像ラテントの方が絶対的な変化量は大きい**。curr_primary (T=3) は完全に
ゼロ変化であり、固定入力の不変性を確認できる（対照群としての健全性チェック）。
画像ラテントノルムはアクションラテントの約2.0〜2.2倍（97〜107 vs 49）であり、ノルム自体の
変化はごく微小（1%未満）——デノイジングはノルムではなく方向（内容）を精緻化していることが
示唆される。

**結論**: 変化量の単調増加という定性的パターンは、行動出力だけでなく画像ラテント生成にも
普遍的に見られる。ただし、テーマ1（§2.1）で示したように、この単調増加という事実だけからは
モデル固有の効果かどうかは判定できない（線形ガウスヌルとの対比が必要）——**本テーマでは
画像ラテントについてこの対比を行っていない**ため、変化量の絶対的な大きさの比較にとどめる。

**注意点・限界**: 本節はepisode単位のブートストラップCIや線形ガウスヌルとの対比を行って
おらず、点推定のみである。成功率も記録されていないため、ロールアウトの質についての
確認ができていない。

**今後の検証案**: 画像ラテントについても線形ガウスヌルとの対比（あるいは画像の場合に
適した非ガウスヌル）を構成し、テーマ1と同様の厳密な統計的検証を行うことが望ましい。

### 8.2 FFT空間周波数解析

**目的**: 将来画像ラテントの空間周波数特性がデノイジング各ステップでどのように変化するかを
確認し、「Coarse-to-Fine（低空間周波数→高空間周波数の順次精緻化）」がラテント空間でも
観察されるかを検証する。

**手法**: future_primary (T=8) のx̂₀の14×14空間マップに2D FFTを適用し、半径ごとに平均
パワーを計算した。k=0〜4の全5ステップを比較した。

**結果（定性的観察）**: 画像ラテント（future_primary, future_wrist）はアクションラテント
（action）に比べて高周波成分を多く含む——画像が空間構造を持つ2Dデータであるのに対し、
アクションは低次元の制御指令を高次元空間に埋め込んだ表現であるためと考えられる。k=0〜k=4
間でスペクトル形状は大きく変わらない——ラテントノルム変化が1%未満であること（§8.1）と
整合する。明確な低周波→高周波の順次精緻化パターンは観察されない。

**解釈**: これは行動空間で確認したFFT解析（§2.2）の結果——Coarse-to-Fineパターンが
明確には支持されない——と定性的に一致する。EDMスケジュールが既にk=0（σ=80）から
全周波数帯域を混在させて予測するためと考えられる。

**結論**: 画像ラテントの空間周波数解析でもCoarse-to-Fine仮説は支持されない。

**注意点・限界**: 本節は定量的な統計検定（テーマ1の§2.2で行ったB_B置換検定に相当するもの）
を行っておらず、プロットの目視観察に基づく定性的な結論である。

**今後の検証案**: 半径方向パワーの定量的な集計と統計検定を追加することが望ましい。

### 8.3 層間表現類似度（CKA）

**目的**: DiTの各プローブ層で、行動トークン（T=5）とfuture_primaryトークン（T=8、生成対象の
将来画像）、およびcurr_primary（T=3、固定の現在画像）の内部表現がどの程度「類似」して
いるかを測定し、生成対象間の表現分化のパターンを明らかにする。

**手法**: 全7プローブ層についてLinear CKA（§3.3で定義したものと同一の指標）を全5
デノイジングステップで計算した。

**結果: action vs future_primary**:

| Block | k=0 | k=1 | k=2 | k=3 | k=4 | 傾向 |
|---|---|---|---|---|---|---|
| Block-0 | 1.000 | 1.000 | 0.997 | 0.952 | 0.818 | k=4で大幅低下 |
| **Block-4** | **0.961** | 0.960 | 0.927 | 0.495 | **0.124** | k=4で急崩壊 |
| **Block-9** | **0.857** | 0.835 | 0.798 | 0.516 | **0.151** | k=4で急崩壊 |
| Block-13 | 0.519 | 0.510 | 0.484 | 0.394 | 0.193 | 低下傾向 |
| Block-18 | 0.399 | 0.399 | 0.398 | 0.405 | 0.439 | ほぼ不変（微増） |
| Block-22 | 0.309 | 0.311 | 0.314 | 0.324 | 0.343 | 微増 |
| Block-27 | 0.139 | 0.140 | 0.143 | 0.151 | 0.162 | 微増 |

**結果: action vs curr_primary（固定入力との比較）**:

| Block | k=0 | k=4 | 傾向 |
|---|---|---|---|
| Block-0 | 0.754 | 0.754 | ほぼ不変 |
| **Block-4** | 0.722 | **0.100** | k=4で急崩壊 |
| **Block-9** | 0.705 | **0.124** | k=4で急崩壊 |
| Block-13 | 0.454 | 0.150 | 低下 |
| Block-18 | 0.145 | 0.149 | ほぼ不変 |
| Block-22 | 0.144 | 0.147 | ほぼ不変 |
| Block-27 | 0.133 | 0.152 | 微増 |

**解釈**: Block-4, 9ではk=0の時点でアクション・将来画像・現在画像のどれも高い類似度
（>0.7）を示す——ノイズが支配的な入力状態では、全T位置の表現がまだ同質であることを示唆
する。k=4で急崩壊する（action vs future_primary: 0.96→0.12、action vs curr_primary:
0.72→0.10）——これは生成対象（行動・将来画像）と固定対象（現在画像）の表現が、デノイジングの
進行とともに分化していくことを意味する。Block-27は全ステップで低い類似度（<0.16）を
示し、最深部では各T位置が完全に独立した専用の表現を持つことを示唆する。Block-18, 22, 27
はCKAが低く（0.10〜0.44）かつデノイジングによる変化が小さい——深い層は最初からT位置ごとに
専用化した処理を持つと考えられる。

**結論**: 中間浅層（Block-4, 9）では「ノイズ状態での表現の同質性→デノイジング終盤での
表現の分化」というパターンが、行動トークンと画像トークンの間のCKAとして観察された。
これはテーマ2（§3.3）で確認した「中間層でのk=3→4のステップ間CKA急落」とは異なる比較
（ステップ間ではなくトークン間）だが、同じ最終ステップで大きな表現変化が起きるという
点で整合的である。

**注意点・限界**: 本節のCKAはブートストラップCIを付与しておらず点推定のみである。

**今後の検証案**: テーマ2と同様のブートストラップCI・複数指標でのクロス確認を行うことが
望ましい。

### 8.4 線形プロービング

**目的**: 画像トークン（future_primary、curr_primary）の隠れ状態がスキル実行フェーズを
線形予測できるかを測定し、行動トークン（テーマ4）と比較する。

**手法**: 全7プローブ層×全5デノイジングステップについてRidge線形回帰（alpha=1.0）を
LOEO CVで評価した。ラベルはcall_idxの3等分（チャンスレベル≈33%）である
（**テーマ4のprogress_3ラベルとは定義が異なる点に注意**——テーマ4はquantile分割で
クラスバランスを均等化しているが、本節は単純な等分割である）。

**結果**:

| 層 | action（T=5） k=0 | action k=4 | future_primary（T=8） k=0 | future_primary k=4 | curr_primary（T=3、固定） k=0 | curr_primary k=4 |
|---|---|---|---|---|---|---|
| Block-0 | 0.429 | 0.453 | 0.414 | 0.438 | 0.456 | 0.454 |
| Block-18 | 0.477 | 0.477 | 0.464 | 0.466 | 0.431 | 0.432 |
| Block-27 | 0.504 | 0.483 | 0.497 | 0.481 | 0.476 | 0.489 |

**解釈**: 全トークン・全層で精度が0.41〜0.51に集中——チャンスレベル（0.333）は上回るが
大幅には届かない。全5ステップ（k=0〜4）を通じて精度の変動は±0.03程度と小さく、**行動
トークン（テーマ4のprogress_3プロービング）が示したデノイジングステップへの明確な依存性
（浅層・中間層でk=4に向けて精度が上昇するパターン）とは対照的**——画像トークンにはスキル
フェーズ情報が少ない、またはデノイジングによって変化しない形で符号化されている可能性がある。
action vs future_primaryの差はほぼなし（精度差<0.01）——CKAが高い（k=0で0.86〜1.00）
ことと整合する。**curr_primary（固定入力、デノイジングされない画像）も同程度の精度**を
示す——画像のスキルフェーズ情報は、生成（デノイジング）によって新たに作られるのではなく、
観測画像から入力段階で既に混入している可能性を示唆する。

**結論**: 画像トークン（future_primary）のスキルフェーズ情報のデコード精度は、行動
トークンと異なりデノイジングステップにほぼ依存しない。また固定入力（curr_primary）と
同程度の精度しか出ないことから、画像トークンの「進行度情報」は主に入力画像そのものに
由来し、デノイジングによって強化されるものではない可能性が高い。

**注意点・限界**: 本節のラベル定義（単純な時間3等分）はテーマ4のラベル定義
（quantile分割）と異なり、クラス不均衡が補正されていない可能性がある。またRidge回帰
（線形回帰）を用いており、テーマ4で確立したLogisticRegression+fold内PCA+BH-FDR補正という
より厳密なパイプラインとは異なる。

**今後の検証案**: テーマ4と同一のパイプライン（fold内標準化PCA+LogisticRegression+
permutationテスト+BH-FDR補正）で画像トークンのプロービングを再実施し、行動トークンとの
比較を統計的に厳密化することが望ましい。また、本テーマ全体（§8.1〜8.4）について、
テーマ3で確立した「PRなどの絶対値がepisode数に依存する」という教訓が画像ラテントの
解析にも当てはまるかを、独立に確認することが望ましい。

---

## 9. 統合結論

複数のテーマにわたる検証結果を統合すると、Cosmos Policy の28-block DiTのデノイジング機構
について、以下の描像がデータに最も忠実である。

**(a) 行動出力レベルでは、素朴な Confidence-to-Commitment は棄却される**（§2.1）。
schednorm正規化後の‖Δx̂₀‖はk=3→4で最大となるが、同一σスケジュールの線形ガウス最適
デノイザ（ヌル）も同様に単調増加し、実測はヌルの48%（σ_data再較正後は38%）しか変化しない。
モデルは最終ステップで線形ガウス最適推定よりも小さい変化しか起こしておらず、これは行動
分布が低次元多様体に集中していることを示唆する。

**(b) 中間層（Blk-4/9/13）の内部表現レベルでは、k=3→4に実質的な表現再編がある**
（§3.1〜3.5）。PRが52〜58から18〜21へ急落（Cohen's d≈1.8〜1.9、大効果量）、CKAも
0.999から0.73前後へ急落し、方向コサインも0.90〜0.94まで低下する。ランダム初期化nullモデルでは
この急落は観察されず、これが学習固有の構造であることを確認した。(a)と(b)は独立の現象であり、
「行動出力の変化が小さい」ことと「中間層の内部表現が大きく再編される」ことは矛盾しない。

**(c) 崖と見えていた形状は、実際には滑らかで連続的な多段階の減少である**（§4.3）。
5-stepスケジュールでは解像度不足のため単一ステップの「崖」に見えていたが、9-step
スケジュールでの再検証（初期のseed固定バグを特定・修正した上での再検証）により、
Blk-4/9/13のPRはσ≈21→4の範囲にわたって連続的に減少することを確認した。

**(d) 条件付け情報（画像+proprio）の常時利用という第3の経路が存在する**（§5.2, §6.2,
§6.3）。深層（Blk-18/22/27）のgripperプローブはデノイジングがほぼ純ノイズの初期段階
（k=0）でも既に高精度（96〜97%）であり、これは「デノイジングによる情報蓄積」だけでは
説明できない。統制実験（T8, T8b）により、単独チャネル（proprioのみ・画像のみ）の
差し替えではプローブ予測は元のまま留まるが、**画像+proprioを同時に一貫した形で差し替えると、
深さに応じて単調にsourceへの追従が強まる**（最深層で85%）ことを確認した。また
current_proprioラベルでの検証により、深層のgripperデコードが「単純な生proprioの通過」
ではないことも確認されている。

**(e) Blk-18は複数の独立解析から一貫して「条件付けへの特異的な感度」を示す層として
指し示される**（§6.4）: 自己注意でのproprioサージ、T8のproprio_swap感度、T8bの
all_swap追従率、J-bのσ依存的な単調減少——4つの独立した解析軸での一致である。

**(f) padトークンへの注意重み集中は、モデルの欠陥ではなくアーキテクチャ上無害な現象で
ある**（§7）。cross-attentionの重みの93〜98%はpadトークンに乗るが、pad位置のK・Vは
T5埋め込みのゼロパディングとbias無し線形層という設計により数学的に厳密にゼロとなるため、
出力への寄与は全層で厳密に0.000000である。

**(g) 画像ラテント（将来フレーム）生成も、行動生成と質的に類似した動態を示す**（§8）。
変化量の単調増加パターン、ノイズが支配的な段階での表現の同質性からデノイジング終盤での
表現分化へという流れは、行動トークンと将来画像トークンの双方に共通する。ただし本テーマの
解析は統計的厳密性（bootstrap CI・permutationテスト）を欠く探索的なものであり、確証的な
結論としては扱わない。

**(h) 有効ランク（PR）の絶対値は、実際にサンプルした episode（シーン）数に強く依存する**
（§4.2）。この量を報告する際は必ず使用episode数を明記し、異なるepisode数・異なるrun
から得られた値を直接比較してはならない。

**避けるべき言い回し**: 「Confidence-to-Commitment」という単一の名称で(a)〜(d)の現象を
一括りにする表現は誤解を招くため使用しない。使うとしても、行動出力レベル・中間層表現
レベル・条件付け経由の情報という、性質の異なる複数の経路のうちどれを指しているかを
必ず明記する。

---

## 10. 既知の限界・未解決課題

- **multi-seed再現性の欠如**: T6/T8/T8b/extended-step/self-attentionの各解析は、いずれも
  単一タスク（PnPCounterToCab）・単一のロールアウト実行に基づく。Blk-18三角測量
  （§6.4）を含め、複数シードでの独立な再現確認は本レポートの範囲では実施していない。
  これは今後の検証における最優先事項である。
- **T8bのbaseline probe精度低下の真因が未解決**（§6.3）: N=360（15episodes）のプールで
  Blk-18/22のbaseline精度が元の確立されたprobe（N=1108, 50episodes）より20pp低い。
  この差はサンプルサイズ・キッチンレイアウト多様性・言語条件付け多様性のいずれでも
  説明できないことを確認したが、真因は特定できていない。より大規模なpoolでの再現、
  および該当ロールアウト自体の成功率・行動品質の確認が今後の課題である。
- **テーマ7（画像ラテント生成）は統計的厳密性を欠く探索的解析**（§8）: bootstrap CI・
  permutationテスト・BH-FDR補正・線形ガウスヌルとの対比のいずれも未適用であり、
  ロールアウトの成功率も記録されていない。テーマ1〜6と同水準の厳密さでの再解析が
  望ましい。
- **一部の自己注意・クロスアテンション解析は成功率0%のrunに基づく**（§6.1補足、
  §7.1前半）: これらは定性的なパターンの参考情報としてのみ扱い、定量的な精度の主張には
  用いていない。成功率が正常なrunでの再確認が望ましい。
- **σ_dataの較正感度**（§2.1）: k=0→1で観測された「1.29倍の超過」という補助的な観察は
  σ_dataの較正選択（実測0.443 vs EDM設計値0.5）に敏感であり、ほぼ消滅しうる。ただし
  中核結論（k=3→4での過小）は較正に対して頑健である。
- **PRの絶対値のepisode数依存性が及ぼす影響の全容が未確認**（§4.2, §3.5, §8）:
  この依存性は行動トークンのPR解析（テーマ2, テーマ3）で確認されたが、ランダム
  初期化nullモデル比較（§3.5、N=10 episodesのみ）や画像ラテント解析（テーマ7）に
  ついても同様の感度分析を行うことが望ましい。

---

## 11. 発見されたバグ一覧

検証の過程で発見された、解析スクリプトおよび検証プロセス自体の実装上の問題を以下に
まとめる（モデル自体のバグではなく、いずれも検証用コードの実装上の問題である）。

| # | バグ | 発覚の経緯 | 影響 | 対応 |
|---|---|---|---|---|
| 1 | **クロスアテンション再構成フックでattention_maskの適用漏れ**（§7.1） | pad位置への重みが最大7.1%という一見小さい値になっていたが、実際にはmaskを適用せず512トークン全体で正規化した値だった | pad重みの解釈自体は最終的に「モデルがマスクを適用しない設計である」という事実と整合していたため実害は限定的だったが、「マスク適用済みの結果」として当初報告しようとした点は訂正が必要だった | 既存データにtoken_attention_maskを事後適用し、実トークン上で再正規化して§7.1の後半（KL・Top-1解析）を実施 |
| 2 | **`extended_step_effrank_jb.py`系列スクリプトでのデノイジング初期ノイズseed固定**（§4.3） | 25episode runと50episode runの両方でPRが2〜4という極端に低い値を示し、episode数増加でも改善しなかったことから発覚 | Blk-4/9/13のPRを本来の50〜56程度から2〜4へと大幅に過小評価（12〜28倍の過小評価）。崖→滑らかの形状判定にも疑いが生じた | `seed=cfg.seed`（全call固定）を`seed=cfg.seed+total_calls`（call毎に変化）へ修正し、2回独立に再実行して結果を再確認 |
| 3 | **`noise_pred_norm`をD_θ（デノイズ後予測）のノルムとして誤ラベリング**（§2.4） | ‖D_θ‖のCVとして報告された0.189%が、独立に測定したF_θのCV（7.62%）と、EDMのスケーリング関係（D_θ≈0.5·F_θ、定数倍はCVを変えない）の下で数値的に両立しないことから発覚 | 「D_θのノルムがほぼ一定」という誤った結論に基づく解釈が行われていた | 実際にはε_θ=(x_t−D_θ)/σのノルムであり、高次元測度集中の数学的帰結（1/√(2d)の理論値と一致）であることを検算で確認し、ラベルと解釈を訂正 |
| 4 | **プローブ精度比較における前処理（PCA標準化 vs 中心化）の未分解**（§4.1） | 「旧」「新」プローブ実装の比較で、当初「異なるrunだった」という誤った結論に至り、後にこれも誤りと判明した根本原因 | 精度差の真因（PCA前処理の選択）が、run差という誤った説明で一時的に覆い隠されていた | 2要因（PCA標準化の有無×Ridge直前の再標準化の有無）のアブレーションにより、PCA標準化の有無のみが差の全量を説明することを直接確認 |

---

## 12. 使用スクリプト・生成成果物一覧

本レポートの各検証で使用した主要なスクリプトと、対応する生成成果物（結果ファイル・図表）を
テーマ別にまとめる。

**テーマ1（行動出力レベルのデノイジング動態）**:
- `mechanism_analysis.py` / `mechanism_reanalysis.py`: Δx̂₀変化量・FFTスペクトル・
  preconditioning解析
- `dimension_analysis.py`: 次元別変化量・グリッパー切替分析
- `precond_ftheta_analysis.py`: F_θ解析（EDM preconditioning前の生ネットワーク出力）
- `effect_size_power.py`: Cohen's d・検出力分析（σ_data感度分析を含む）

**テーマ2（中間層内部表現の構造的崩壊）**:
- `layer_analysis.py`: 有効ランク（PR）・特徴ノルム・方向コサイン・CKA解析
- `null_model_analysis.py`: ランダム初期化nullモデルとの比較

**テーマ3（PRのepisode数依存性とrun固有交絡）**:
- `t6_decompose_confound.py`: T6の2×2×2アブレーション（第1段階、PCAリーク×分類器×n_comp）
- `t6_pca_scaling_ablation.py`: T6のPCA前処理2要因アブレーション（第2段階、真因特定）
- `pr_episode_curve.py`: PRのepisode学習曲線
- `diag_env_reset_diversity.py` / `diag_image_diversity.py`: キッチンレイアウト・画像
  多様性の診断（否定された仮説の検証用、GPU/モデル不要）
- `extended_step_effrank_jb.py`: 9-stepスケジュールでのPR/J-b解析（seedバグ修正済み、
  `seed=cfg.seed+total_calls`）
- `confirm_seed_rootcause.py`: seed固定バグの根本原因確認（同一観測集合を固定seed/
  変化seedの両方で通しPRを比較）

**テーマ4（線形プロービング）**:
- `linear_probe.py`: fold内PCA+LogisticRegressionによる線形プロービング

**テーマ5（条件付け情報の伝播経路）**:
- `attention_analysis.py` / `attention_analysis_v2.py`: 自己注意の行正規化Tマトリクス解析
- `t8_fixed_input_control.py`: T8固定入力コントロール
- `t8b_info_source_test.py`: T8b情報源テスト（current_proprio / action_gripper 両ラベル対応）
- `t8b_samplesize_check.py`: T8bの精度低下がサンプルサイズの限界で説明できるかの検証
  （否定された）
- `diag_lang_diversity.py`: 言語条件付け（task description）の多様性診断（否定された仮説の
  検証用、GPU/モデル不要）

**テーマ6（言語クロスアテンションとpadトークン）**:
- `crossattn_analysis.py`: クロスアテンション収集・解析
- `crossattn_masked_analysis.py`: pad マスク適用後の再正規化解析
- `t_pad_attn_verify.py`: pad由来出力寄与のフック再構成 vs 実出力の直接数値照合

**テーマ7（画像ラテント生成）**:
- `image_analysis.py`: 将来画像ラテントのΔx̂₀・ノルム・FFT・CKA・線形プロービング解析

**共通・サニティ**:
- `feature_analysis.py`: 全テーマで用いる行動トークン特徴量収集の基盤スクリプト
  （正しいseed実装 `seed=cfg.seed+ep_idx+t` の参照実装）
- サニティ・成功率ゲート関連スクリプト（T1/T2/T4/T5/T7/T9/T10、G1-G3の実装）









